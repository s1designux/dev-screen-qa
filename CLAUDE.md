# 검수 시스템 — 프로젝트 컨텍스트 (CLAUDE.md · AGENTS.md — 같은 파일)

이 문서는 "디자인 시안 대 개발화면 검수 시스템"의 **확정된 설계 결정**이다.
코딩 에이전트는 이 결정들을 재논의하지 말고 전제로 삼는다.
특히 4번 "하지 말 것"을 반드시 지킨다. 지시가 모호하면 여기 원칙에 맞춰 판단하고, 그래도 애매하면 멈추고 물어본다.

---

## 1. 프로젝트 목적

- 디자인 시안과 실제 개발화면이 동일하게 구현됐는지 검수한다.
- 검수 결과·수정 이력을 **데이터로 축적·관리**한다. (검색·통계·차수 연결이 목적)
- 단순 이미지 비교 도구가 아니라 프로젝트별 검수 데이터 관리 시스템이다.
- 대상: PC 웹 / 모바일 웹 / Android / iOS.

---

## 2. 절대 원칙 (North Star) — 절대 어기지 않는다

1. **데이터가 원본이다.** 검수 결과는 구조화된 데이터(DB)에 저장하고, HTML·Figma·PDF는 그 데이터를 렌더링한 출력물일 뿐이다. Figma 파일이나 PDF를 원본으로 삼지 않는다.
2. **자동 검수는 오류를 "확정"하지 않는다.** "후보"만 생성하고, 최종 확정은 항상 사람(디자이너)이 한다. 신뢰도 점수를 함께 제공한다.
3. **이력은 삭제하지 않는다.** 해결된 오류도 지우지 않고 상태만 바꾼다. 화면에서 숨길 수는 있으나 데이터는 남긴다.

---

## 3. 확정된 아키텍처 결정

- **중심은 HTML 포털.** 운영·데이터 관리·커뮤니케이션의 허브. Figma는 얇은 보조, PDF는 특정 시점 스냅샷(공식 결과서).
- **순서: 검수 데이터화가 자동 검수보다 먼저.** 데이터/이력 기반이 없으면 자동 검수 결과가 갈 곳이 없다.
- **웹과 앱: 데이터·이슈 포맷은 통일, 수집·비교 엔진은 분리.** 하나의 엔진으로 억지로 합치지 않는다.
- **앱 검수는 완전 자동이 아니라 "OCR 보조 + 디자이너 확정".** 한국어 UI이므로 OCR은 PaddleOCR 사용.
- **화면 ID는 2단계.** 디자인 기준 ID ↔ 개발 실행 키를 포털에서 1회 매핑. 개발자가 코드에 디자인 ID를 넣게 하지 않는다.
- **검수 정책은 3계층 상속.** 시스템 기본값 → 화면별 예외 → 요소별 예외. 디자이너가 평소 거의 설정하지 않게 한다.
- **UI 사양서 시스템과 검수 시스템은 공통 화면 ID로 느슨하게 연결.** 처음부터 한 스키마로 합치지 않는다.
- **Figma 연동: 읽기는 REST/링크 붙여넣기, 쓰기(검수보드 역생성)는 플러그인(나중).**

---

## 4. 하지 말 것 (범위 가드레일)

- ❌ 포털 전체를 Figma 플러그인 안으로 옮기기
- ❌ **피그마 검수기 플러그인 (2026-09-14 river 확정 — 폐기).** 검수는 **포털에서만** 한다.
  2026-09-01에 예외로 승인했던 `plugin-image-qa/` 검수기 1.0은 여기서 끝낸다. 파일은 지우지 않고
  `legacy/plugin-image-qa/`로 옮겨 이력으로만 둔다 — **현재 작업에서 부르거나 읽지 않는다.**
  비교 엔진 한 벌은 `engine/ui.html`로 옮겨 살아 있고 포털이 그대로 돌린다(폐기한 것은 창이지 엔진이 아니다).
  `mvp0/policy_api.py`(검수기↔포털 규칙 배선)도 부르는 쪽이 없어졌다 — 파일은 두되 사용 안 함.
- ✅ **남는 피그마 통로 하나:** 시안을 포털로 보내는 촬영 준비 플러그인(프레임 골라 보내기). 검수기가 아니라 배달부다.
  (river 확정 2026-09-14: 시안 통로는 플러그인 유지)
- ❌ 기존의 "포털 이슈 → Figma 보드 역생성" 플러그인 — 여전히 나중 단계
- ❌ 웹 자동 수집·비교 엔진 (Playwright 자동화는 PoC 통과 후)
- ❌ 앱 구조 추출 (ADB/UI Automator/접근성 트리는 별도 검증 스파이크. 기본은 스크린샷+OCR)
- ❌ UI 사양서 + 검수결과서를 하나의 거대 스키마로 통합
- ❌ 오류를 자동으로 "확정" 처리
- ❌ Figma 파일에 검수 데이터·대용량 이미지 저장
- ❌ 모든 요소에 강제 ID 부여
- ❌ 여러 화면·여러 해상도 동시 지원 (MVP0은 단일 화면 흐름으로 검증)
- ❌ 버전 확정/발행 기능, 로그인·다중 사용자 (정책만 확정, 구현은 포털 뒷 단계 — 17번 참조)

새 기능을 만들고 싶으면 먼저 멈추고, 현재 단계 범위인지 확인받는다.

---

## 5. 빌드 순서 (현재 단계: **검수 정책 규칙화 — 이미지 검수 플러그인 1.0의 다음 조각**)

- MVP0 (완료): 데이터 모델 + 수동 기반 검수 데이터화. 실제 검수 1건(15건) 변환 검증 완료.
- **포털 MVP0 (현재):** mvp0 데이터를 읽어 보여주는 로컬 포털 — 화면 목록·필터·상세(좌우 2단)·상태 변경·A4 반출.
- **2026-09-01 사용자 승인 → 2026-09-14 종료:** `plugin-image-qa/` 검수기 1.0은 폐기(4번 참조). 검수는 포털에서만 한다.
- 다음: 포털에서 직접 네모 치기(주석 작성).
- 이후: 검수 정책 3계층 + 동적/제외 영역 → 웹 자동 수집·비교(Playwright, PoC 통과 후) → 개발 커뮤니케이션 확장 → 앱 OCR 트리아지 / 앱 구조 추출(스파이크) → 사양서 연동·통계.

---

## 6. 데이터 모델

MVP0 핵심 엔티티. (Capture / DevelopmentBuild 등은 필드만 남기고 최소화, 나중 확장)

- **Project**: `uuid, name`
- **Screen**: `uuid, project_id, human_key(예: CV-WEB-012), name, platform, dev_keys[], states[], variants[]`
- **DesignVersion (스텁)**: `screen_id, file_key, node_id, dev_capture_node_id`
- **ElementMapping**: `screen_uuid, design_node_id, dev_element_key` (디자인 노드 ↔ 개발 요소, 1회 매핑)
- **InspectionRun**: `uuid, screen_id, round(1/2/3), inspector, created_at, pass_fail`
- **InspectionIssue**: `uuid, screen_id, run_id, logical_element_key, box{x,y,w,h}, category, properties[], expected, actual, description, severity, status, found_round, resolved_round, dedup_key`
- **IssueHistory**: `uuid, issue_id, from_status, to_status, actor, at, note` (append-only, 삭제 금지)
- **ComparisonPolicy**: `scope(system/screen/element), target, mode` (MVP0에선 스텁)

**오류 상태값**: 발견 / 개발확인 / 수정예정 / 수정완료 / 재검수필요 / 검수완료 / 보류 / 오류아님 / 재발
**미해결 집합(constants)**: 발견 / 개발확인 / 수정예정 / 재검수필요 / 재발 (수정완료는 경계값 — 실데이터 확인 후 재검토)

**dedup_key (차수 간 동일 오류 연결)**: `{화면}|{요소식별자}|{규칙}|{속성}`
재검수 시 새 오류를 만들지 않고 이 키로 기존 오류를 찾아 상태만 갱신한다.

**properties**: 실제 검수는 "핀 1개 = 요소 1개 + 속성 여러 개"다. 이슈에 속성 배열로 저장한다.

---

## 7. 화면·요소 ID 규칙

- **사람이 읽는 키와 내부 UUID를 분리한다.** 사람키는 라벨·조회용(바뀔 수 있음), 참조 무결성은 UUID가 담당.
- 화면 사람키: `{SERVICE}-{PLATFORM}-{NNN}` (예: `CV-WEB-012`).
- 화면 상태는 접미사: `CV-WEB-012@empty`. 디바이스/해상도는 화면 ID에 넣지 말고 **변형(variant)** 으로 분리.
- 개발 실행 키: 코드에 이미 있는 값 그대로 사용 (웹 route, Android Activity, iOS ViewController). 화면 UUID에 매핑.
- 요소 논리키: `{화면}+{역할/컴포넌트}+{텍스트해시}+{순번}`. 명시적 QA/접근성 id가 있으면 그것이 최우선.

---

## 8. 검수 정책 4계층 (2026-09-11 첫 조각 구현 — `mvp0/policy.py`)

`시스템 기본값 → 서비스(프로젝트) → 화면별 예외 → 요소별 예외` 순으로 덮어쓴다.
(river 확정 2026-09-11: 서비스마다 같은 것·다른 것이 있어 **서비스 층**을 넣는다. 원래 3계층에 한 층 추가.)
- 규칙의 **알고리즘**은 검수기 파일(`engine/ui.html`)에, **값**(켜고 끄기·기준값·예외)은 포털 DB `policy_rule`에.
  포털이 원본, 피그마 프레임의 설정(qa_settings)은 거울 — 포털 행이 있으면 포털 행이 이긴다.
- 값은 지우지 않고 행으로 쌓는다(append-only). null = 그 층의 예외를 거둠.
- 페이지 상세에서 후보를 가변·제외로 내리면 **요소 층에 자동으로 쌓여** 다음 차수·다음 시안에서도 기억한다.
- 화면: `/policy` (시스템) → `/policy/service/<uuid>` → `/policy/screen/<uuid>`.
- **검수기 ↔ 포털 배선 (2026-09-14 완료 · 같은 날 플러그인 폐기로 사용 안 함, `mvp0/policy_api.py`):** 검수기는 비교 전에
  `POST /api/policy/read`로 그 화면의 규칙을 받아 겹치고, 가변/고정·화면 종류·검수 범위를 바꾸면
  `POST /api/policy/write`로 포털 층에 쌓는다. 화면은 (파일 열쇠, 프레임 id)로 찾고, 못 찾으면
  프레임 이름이 **꼭 하나만** 맞을 때만 인정한다. 포털이 꺼져 있으면 조용히 넘어가고 피그마 프레임에만 저장한다.
  파일 이름은 NFC로 맞춰 본다(macOS를 거친 이름은 자모가 풀려 있어 안 맞춰 두면 같은 파일을 못 알아본다).
- **판정↔규칙 연결 기록 (2026-09-14 완료, `mvp0/rule_log.py`):** 검수기가 후보마다 **어느 규칙이 그 자리에 뒀는지** 이름표를 붙이고
  (제외표·가변표·목록표의 id), 사람이 제외·가변·되돌림·지적 등록을 누를 때마다 `rule_verdict`에 한 행씩 쌓는다(append-only).
  엔진이 둔 자리와 같으면 agree, 다르면 overturn. `/policy`·`/policy/screen/<uuid>` 맨 아래 **규칙 성적** 표에
  규칙별 [나온 후보 · 그대로 둠 · 뒤집음 · 맞은 비율]이 보인다. **여기서 규칙을 자동으로 고치지 않는다**(세기만 한다).
- 다음 조각(대기열 0-④⑤): 규칙 발굴 엔진(로컬) / 제안+검증 숫자.
모드: `Exact / Text Exact / Style Only / Layout Only / Structure / Dynamic / Ignore`.
기본값 예: 지도 타일=Ignore, 지도 컨테이너=Layout Only, 사용자명=Style Only, 날짜=Dynamic, 버튼·아이콘=Exact.

**검수결과서 범위 규칙 (To-Be, 2026-07-21 확정):**
개발화면 검수결과서는 **"눈에 보이는 시각적 차이"만** 다룬다.
- **포함(검수 대상):** 흐름/구조 차이(디자인에 없는 요소 추가·누락 등), 주요 버튼 표시 차이, 색상(배경색·글자색·테두리색), 크기(높이·너비), 타이포(글꼴·글자크기·글자굵기), 모양(모서리 둥글기).
- **제외(검수결과서에 넣지 않음):** 컴포넌트 **안쪽여백(padding)** 등 미세 간격. 이런 건 **퍼블리싱 단계**에서 걸러진다.
- 한 이슈에 여백+크기/색상이 섞이면 **여백 속성만 제거**하고 나머지는 유지. 여백뿐인 이슈는 **삭제하지 않고** status '오류아님'(사유: 퍼블리싱 소관)으로 재분류하고 이력을 남긴다(2번-3 이력 삭제 금지).
- 적용 예: 실데이터 TB-WEB-001에서 6개 이슈의 안쪽여백 제거, #6 상단바(여백뿐)는 '오류아님' 처리 → 미해결 16→15.

---

## 9. 웹/앱 검수 분리 원칙 (지금은 구현 안 함, 설계만 고정)

- 웹(나중): DOM/CSS 구조 비교 우선(픽셀 아님) → 후보 생성.
- 앱(나중): 스크린샷 + PaddleOCR + 색상·영역·레이아웃 비교 → 후보 생성 → 디자이너 확정.
  - Android는 협조 없이도 ADB `uiautomator dump`로 구조 일부 추출 가능성 있음(검증 대상).
  - iOS는 개발 협조 없이는 구조 추출 어려움. 이미지+OCR 기본.
- **웹·Android·iOS를 하나의 정확도 목표로 잡지 않는다.**

---

## 10. Figma 연동 경계

- **읽기(Figma → 포털):** REST API 또는 프레임 링크 붙여넣기(링크에서 file_key·node_id 추출, node-id 하이픈→콜론 변환)로 이미지·노드값을 당겨온다.
- **쓰기(포털 → Figma 검수보드 역생성):** 플러그인만 가능. 나중 단계 전용.
- 확인 필요(포털 구현 시): **디자이너 PC가 아니라 포털 서버가 Figma에 직접 닿는지**. 안 닿으면 링크 붙여넣기/플러그인 경유로 우회.

---

## 11. 기술 제약

- 폐쇄망(인트라넷), 오프라인 로컬 실행. **유료 외부 API 사용 금지.**
- OCR: PaddleOCR (한국어). / 웹 캡처(나중): Playwright.
- 저장소: SQLite (파이썬 표준 내장, 오프라인). 원본=DB, 확인용 JSON 내보내기 병행.
- 모든 라이브러리는 오프라인 설치 가능 여부를 먼저 확인.
- **이 컴퓨터에서만 쓰는 값은 `설정.json` 한 장에.** (river 확정 2026-09-14)
  저장소가 공개라 실제 주소·계정·자리(경로)를 코드에 적지 않는다. 포트·자료함 자리·피그마 열쇠·adb 자리·정본 저장소는
  뿌리의 `설정.py` 를 거쳐 읽는다 — 찾는 차례는 `설정.json` → 환경변수 → 기본값이라 기존 환경변수 방식도 그대로 산다.
  `설정.json` 은 깃에 올리지 않고 `설정.예시.json` 만 올린다. 지금 쓰는 값은 `python3 설정.py` 로 본다.
  내부망 PC도 깃허브 내려받기는 된다(올리기만 막힘) — 정본은 평소대로 받아온다.
  `정본.이미받은자리` 는 혹시 `raw`·`codeload` 가 따로 막혔을 때만 쓰는 예비 통로다.

---

## 12. 작업 규율

- `git add .` 금지. **선택 스테이징만.**
- 커밋 전 **멈추고 보고(stop-and-report)**, 자체 판단으로 커밋하지 않는다.
- **만든 것을 스스로 검증하지 않는다 (maker ≠ verifier).** 검증은 분리된 주체가 한다.
- 자동 검수 결과를 스스로 "확정"으로 승격하지 않는다.
- **윈도우용 `.bat` 실행 줄에는 한글을 넣지 않는다 (ASCII만).** cmd.exe 가 배치 파일을 바이트 위치로 되짚어 읽는데
  한글을 글자 수로 세어 명령이 쪼개진다(`powershell` → `powe`+`rshell`). 인코딩(UTF-8/949·chcp)으로 풀리지 않는다.
  **한글 안내와 실제 작업은 `scripts/*.ps1`(UTF-8 BOM 필수)로 빼고, `.bat` 은 그 ps1 을 부르기만 한다.**
  `%~dp0` 는 끝에 역슬래시가 있어 `"%~dp0"` 로 넘기면 따옴표를 먹는다 — `"%~dp0."` 로 넘기고 ps1 에서 `Resolve-Path` 로 편다.
- **회사 윈도우 PC에는 관리자 권한이 없다.** `schtasks /SC ONLOGON` 은 쓸 수 없다(Access is denied).
  자동 시작은 '시작프로그램' 폴더 바로가기로 건다. `schtasks /SC DAILY`(백업 예약)는 일반 권한으로 된다.
- 예약·등록 스크립트는 **실패를 성공이라고 안내하지 않는다.** 종료 코드를 보고 성공/실패를 갈라 말한다.
- **좌표를 바꾸는 셈은 `자.py` 를 거친다** (river 확정 2026-09-16).
  시안 px · 개발 촬영본 px · 핀 그림 단위 · 넓은 판 기준 자리 · 화면 기준 자리 — 자가 다섯인데 곱셈이
  코드 곳곳에 흩어져 있어 새 기능마다 하나씩 잊었다(홈 화면 2240px 밀림 · 나란히 보기 1.4배).
  **기준 자는 '화면 기준 px'(시안 한 판의 좌표계) 하나**다. 브라우저 쪽은 `자.JS()` 가 같은 셈을 내준다.
  새 코드에서 `naturalWidth/` · `artboardWidth` · `contentX` 로 직접 나누거나 더하지 않는다.
  고치면 `python3 scripts/자점검.py` 로 본다 — 접수된 화면마다 시안과 개발이 같은 자 위에 있는지 짚는다.
- **화면에 글을 적을 때는 `docs/화면글쓰기규칙.md` 를 따른다** (river 확정 2026-09-16).
  한 가지는 한 번만 · 안내는 두 줄까지 · 문서체 금지 · 말투는 `~합니다` 하나로 · 이름은 이름표대로
  (확정된 것은 **수정필요**, '지적'은 쓰지 않는다). 화면 글을 고치거나 새로 만들면 커밋 전에
  `python3 scripts/글점검.py` 로 옛 이름·문서체·말투 섞임·긴 안내를 본다(짚기만 한다, 자동으로 고치지 않는다).

---

## 13. 완료 기준 (데이터화 검증 — 완료됨)

과거 검수 1건을 데이터로 넣었을 때: ①차수 간 오류 연결(dedup) ②해결 숨김·데이터 유지 ③미해결만 추출 ④가로 A4 결과서 재생성. 합성 픽스처로 4개 모두 검증 완료. 실제 검수 1건(TB-WEB-001, 15건)도 로드·조회·미해결 필터 검증 완료.

---

## 14. 현재 파일 구조 (mvp0/)

`schema.sql`(엔티티+properties+design_version 스텁) / `constants.py`(상태값+미해결 정의) / `db.py` / `load_fixture.py`(두 픽스처 모양 처리) / `queries.py`(미해결 조회) / `report.py`(가로 A4) / `export_json.py` / `fixtures/`(cv-web-012 합성, tb-web-001 실제 15건).
생성물(`*.db`, `export.json`, `report.html`)은 `.gitignore` 처리, 커밋하지 않는다.

---

## 15. 목적지 (작업 스타일) — 확정

- 최종 목표: **검수와 데이터 관리를 포털 한 곳에서** 한다. Figma·PDF는 유관부서에 반출(공유)할 때만 생성하는 출력물이다.
- 전환은 단계적. 지금은 검수 입력을 Figma에서 데이터로 옮기는 경로가 이미 검증됨(실제 15건). 포털은 그 데이터를 **관리하는 화면**부터 만든다.
- **포털에서 직접 네모 치기(주석 작성)** 는 목적지에 포함되나, 포털 뼈대 **다음** 조각으로 만든다.

---

## 16. 기존 검수결과서 양식 → 포털 전환 원칙

기존 Figma 양식(표지·간지·양식·이력)을 그대로 이식하지 않는다. 네 갈래로 분류해 처리:

- **버림(레이아웃·장식):** 표지·간지 비주얼, A4 가로 시트 틀, 로고·워터마크·페이지번호. 'PDF 문서'라서 생긴 포장이므로 포털에서 재현하지 않는다.
- **데이터로 보존(정보):** 스토리보드 ID·화면명·검수일·차수·Pass/Fail, 오류 내용·위치·날짜별 이력.
- **작업 화면으로 보존(레이아웃):** 좌(디자인)·우(개발) 2단 + 그 위 네모·번호·쪽지. 검수 상세 화면에서 이 배치를 유지한다.
- **반출용으로만 재생성(PDF):** 표지·간지·A4 시트는 '반출' 시에만 데이터로부터 조립한다.

참고: 기존 양식은 이미 '해결 항목을 지우지 않고 숨김' + '날짜로 차수 추적'을 수작업으로 하고 있었다. 포털은 이를 자동화하는 것이지 새 방식을 강요하지 않는다.

---

## 17. 버전·이력 정책

원칙: 모든 변경은 **자동 기록**(무엇을 남길지 판단하지 않음). 이력서에는 **주요 변경만 표시**(경미한 변경은 접어둠, severity로 자동 분류). 버전 번호는 시스템이 **자동 제안, 필요 시 수동 조정**.

**버전 규칙 (조직 관례 기반):**
- 작업 중 자잘한 수정: 숫자 버전을 매기지 않는다. 진행은 검수 차수(1·2·3차)로 추적.
- 개발그룹 공유(완료 전, 통상 4~5회): 발행할 때마다 `0.1 → 0.2 → …` 하나씩 올린다. 자잘한 수정으로는 오르지 않으므로 숫자가 바닥나지 않는다.
- 과제 완료: `1.0`. 시스템은 '1.0 제안'만, 확정은 사용자가 수동.
- 완료 후 추가개발: 양적 기준 자동 제안 — `1.1`(소수 화면 수정) / `2.0`(다수 화면 변경, 임계값 5개↑·신규 기능/화면·전면 리뉴얼). 임계값은 설정값. 최종 번호는 사용자 확정, 수동 조정 시 사유를 로그에 남김.

**버전 확정과 PDF 출력 분리:**
- 'PDF 미리보기/내보내기'는 버전에 영향 없음. 몇 번이든 자유(테스트 포함).
- 버전은 '**버전 확정(공유본 발행)**' 버튼을 누를 때만 오른다. 이때 다음 공유 번호로 증가 + 스냅샷 고정 + 이전 공유본 대비 변경요약(해결/재발/신규) 자동 생성.
- 즉: **출력 = 언제든 자유 / 발행 = 확정 버튼을 누른 그 순간만.**

**버전 두 종류 구분:**
- 디자인 원본 버전: 시안 자체가 바뀔 때만.
- 검수결과서(문서) 버전: 위 규칙(0.x 공유 → 1.0 → 1.1/2.0).

**주의:** 위 버전·발행 기능은 **정책만 확정**한 것으로, 지금 단계에서 구현하지 않는다(포털 뒷 단계).

---

## 18. 사양 포털 연결 방침 (미래 — 지금 구현 안 함)

별도의 '사양 포털'(검수 전 기획·UI 사양 데이터)과는 코드를 합치지 않고,
얇은 '화면 대응표'로만 느슨하게 연결한다.
- 대응표가 잇는 것: 사양 포털 (projectId + SCREEN-id) = 검수 포털 human_key = 개발 route
- 두 포털은 각자 이름 체계를 그대로 유지한다. 한쪽을 상대에 맞춰 바꾸지 않는다.
- 사양 포털의 code 필드는 건드리지 않는다. 개발 route는 대응표에 별도 칸으로 둔다.
- 이 대응표는 지금 만들지 않는다(양쪽이 모양을 갖춘 뒤 붙임). 지금은 방침만 확정.
- 준비만: 검수 포털의 Screen 데이터에 사양 포털 SCREEN-id를 담을 빈 칸
  (spec_screen_ref, 기본 null)을 하나 둔다. 지금 값은 채우지 않는다.

---

## 19. 재검수 차수 연동 방침 (미래 — 지금 구현 안 함)

개발이 지적을 개선해 개발화면을 다시 보내면 그 화면을 **'새 버전'으로 받는다.**
단 **이슈(지적)는 이미지와 함께 갈아끼우지 않는다** — 이슈는 차수를 따라 이어진다.
즉 **이미지는 버전마다 교체되지만, 이전 차수의 지적 목록은 다음 차수로 '끌고 온다'.**

다음 차수에서 각 지적의 처리(사람이 판단하는 것 전제, 자동 비교는 나중):
- 고쳐졌으면 → **'해결' 후보**
- 안 고쳐졌으면 → **'미해결' 유지**
- 새로 보이면 → **'신규'로 추가**

이는 처음 설계 원칙("해결해도 지우지 않음 · 차수로 추적" — 2번-3, dedup_key)과 **같은 것**이다.
이슈를 차수 간 잇는 축은 이미 있는 `dedup_key` / `found_round` / `resolved_round`다.

**전제(그래서 지금 구현 안 함):**
- 이 연동은 **'실제 이미지 연동'** 과 **'차수 개념이 화면에서 도는 것'** 이 먼저 있어야 성립한다.
  둘 다 아직 나중 조각이라, 이 차수 연동도 그 뒤에 만든다.
- 지금은 **방향만** 문서에 남긴다. 구현·데이터·스키마·화면 변경은 하지 않는다.

---

## 20. 앱 개발화면 캡처 기준 방침 (미래 — 지금 구현 안 함)

**배경:** 개발 이미지 핀 정렬에서 **배율(크기)** 은 비율(%)로 해결됐다(19번 앞 조각).
그러나 **잘린 범위·섞인 요소·스크롤 위치**는 이미지만으로 자동 판별이 안 된다 → **사람이 기준을 지켜야** 한다.
- 웹: 개발자도구 풀페이지 캡처 등으로 **기준 강제가 쉬움**.
- 앱: 사람이 기기로 찍어 **변수가 큼** — 기기별 화면 크기 상이, 상단 상태바·하단 제스처바 섞임,
  스크롤 위치 제각각, 찍는 사람이 여럿이면 통제 어려움.

**대응(3겹, 앱 검수 실착수 시 구현):**
- **(1겹) 캡처 가이드:** 상태바·제스처바 제외, 스크롤 맨 위, 원본 배율 그대로,
  좌표 기준과 **같은 프레임·잘림**으로 촬영. 기기·해상도 기록.
- **(2겹) 업로드 시 확인 절차:** 올릴 때 '상태바 제외했는지 / 원본인지 / 스크롤 위치' 체크받기.
- **(3겹) 겹쳐보기:** 좌·우(또는 기준선) 오버레이로 정렬을 **눈으로** 확인 → 어긋나면 즉시 발견.

**전제(그래서 지금 구현 안 함):**
- 지금은 앱 검수 실착수 전이라 실제 앱 이미지로 검증할 수 없다.
- 이 방침은 **앱 검수를 실제로 시작하는 조각**에서 캡처 가이드·업로드 체크로 구현한다.
- 지금은 **방향만** 남긴다. 코드·화면·스키마 변경 없음.

---

## 21. 다음 작업 대기열 (현재 위치 + 우선순위)

**여기까지 완료(커밋됨):**
- 포털 UI 3층 재구성: 화면 목록 → 검수 페이지 목록 → 페이지 상세
- 페이지 상세: 핀↔카드 uuid 연동, 오류 유형색, 유형 탭, 카드 다중 열, 비교 영역 상단 고정
- 통과 처리(**협의통과**) + 사유 필수 + 이력 append(누가·언제·왜) + **처리됨 탭** + 처리핀 흐림
- 실제 이미지(PNG) 업로드 + **핀 비율(%) 좌표**(배율 무관)
- 재검수 차수 연동 **1차**: 차수별 개발 이미지 / `issue_history.round` / 차수 선택 UI /
  차수 시점 상태 재구성 / 비교 틀 폭 고정

**로컬 AI 검토 결론 (2026-09-07 확정) — 재논의하지 않는다:**
로컬 비전 AI는 **사용자에게 배포하는 기능이 아니다.** 검수기 제작 담당자가 **새 검수 규칙을 발굴·검증**할 때만 쓴다.
사용자는 검수기만 쓰고, 배포하는 것은 AI가 아니라 **규칙**이다. (근거: 화면당 9분 / 아이콘 판독 실패 /
없는 글자 생성 / 가변 자리 판단 불가 / 설정을 함께 배포해야만 재현됨. 2번-2 "자동 검수는 확정하지 않는다"와도 일치.)
정렬(겹치기) 조각은 **완료·커밋됨**(기준 요소 5개→화면 전체 격자, 화면 5개 회귀 확인).

**포털 자동 검수 1차 (2026-09-10 완료):** 페이지 상세를 열면 그 차수의 **후보**가 바로 보인다.
엔진은 `engine/ui.html` 한 벌을 포털이 `/engine/ui.html`로 내보내 숨은 iframe에서 돌린다(복사 없음, 규칙 고치면 양쪽 반영).
디자인 요소는 Figma REST 프레임 트리에서 읽고(`mvp0/figma_elements.py` = code.js collectDesign과 같은 모양), 사람이 검수기에서 정한 설정(공유 칸)을 그대로 쓴다.
저장: `auto_run` / `auto_candidate` / `auto_candidate_event`(append-only) / `design_elements`. **후보는 지적이 아니다** — 사람이 '지적 등록'을 눌러야 `inspection_issue`가 생긴다(2번-2).
같은 입력으로 플러그인 재현과 후보 목록 동일(`validation-image-qa/repro/portal_parity.js`, 체류시간 정답 7/7).
페이지를 여는 것만으로는 DB에 쓰지 않는다(첫 실행·저장은 페이지 JS의 POST).

**검수 시안 바로 받기 (2026-09-10 완료):** 촬영 준비 단계를 거치지 않고, 검수 화면이 이미 있는 프레임의 시안만 갈아끼운다.
촬영 준비 플러그인(`dev-screen-qa-capture/capture-app/plugin-pick`)이 고른 프레임을 두고 포털에 묻고(`POST /api/frames`),
검수 화면이 있으면 단추가 **'검수 시안 바꾸기'** 로 바뀌어 그림·요소(collectDesign 모양)·설정을 `POST /api/design`으로 보낸다. **Figma 토큰 불필요.**
포털: `mvp0/design_receive.py` — 새 판은 `intake_design`에 쌓고(옛 판 유지) `page_design_link`가 페이지→최신 판, `page_design_event`(append-only)에 이력.
`auto_run`은 (차수, 시안)별이라 시안이 바뀌면 후보를 다시 찾고 옛 결과는 남는다. 사용자 이름은 나누지 않는다(통합/개별 구분은 받는 쪽이 한다).
**개발 화면 변경(촬영기로 들어온 페이지, 2026-09-10):** 페이지 상세 오른쪽 '개발 화면 변경' → 같은 접수함에서 찍은 사진 중 고르는 팝업
(디자인 먼저 흐름의 팝업과 같은 모양·추천). 지적이 없을 때만 그 차수 개발 이미지를 바꾸고(`POST /intake/<batch>/capture`, `intake_event` 이력),
지적이 있으면 '바꾸려면 새 차수'로 막는다(대기열 2번 조각).
촬영기가 바로 넣은 페이지(접수함 연결 없음)는 같은 화면 묶음의 사진(`inspection_run.dev_img`) 중 고르며 `page_capture_event`에 이력.
**앱 흐름 완결(우비스 버스):** 촬영 준비 플러그인 '원본 인식하기'가 검수 요소·설정도 동봉 → 촬영 준비 사이트 `site/intake.py`가 접수 때
`intake_design`+`design_elements`+`page_design_link`까지 써 넣어, 포털에서 페이지를 열면 후보가 바로 뜬다(토큰 없음).

**앱 화면 기본 규칙 + 포털 검수 범위 조정 (2026-09-10 완료):**
검수 정책표에 **촬영본 제외표(`CAPTURE_EXCLUDE_RULES`)** 를 더했다 — 세로가 긴 촬영본(앱)은 시안과 무관하게
**기기 상태바 / 화면 키보드 / 하단 내비게이션**을 이미지만 보고 먼저 뺀다(정렬에 기대지 않음). 잘라낸 아래쪽에 해당하는 시안 요소는
비교하지 않고 개수만 알린다(키보드 내린 화면을 더 찍으라는 안내). 정렬은 표가 엇비슷한 무리가 여럿이면 그림 전체 윤곽 점수로 고른다
(키보드에 가린 화면에서 아래쪽 요소가 엉뚱한 자리에 붙어 표를 모으던 문제). 검증: 자체검사 193, 웹 화면 10개 회귀(체류시간 정답 7/7 유지),
우비스 버스 로그인 4쪽(헛지적 17→6·19→10, 정렬 ty 117→20).
포털: 자동 검수 탭 맨 위 **'검수 범위' 카드 + 조정**(개발 화면 위 붉은 선 두 개 끌기 → '이 범위로 다시 검수' / '자동으로 되돌리기').
사람이 정한 값은 `auto_range`(append-only)에 남고 엔진에 topTrim/bottomTrim으로 간다. 다시 검수하면 `auto_run`이 새 회차로 추가되며
(같은 차수·시안에 여러 회차, 최신 표시) 옛 회차·후보는 남는다. 옛 표 모양은 `auto_inspect.repair()`가 외래키를 끈 별도 연결에서 고친다.

**값 대조 후보도 포털에 (2026-09-14 완료, `mvp0/value_candidates.py`):** 값 대조(`valueqa/`) 결과가 수정요청서(문서)로만 나가던 것을
**그림 검수와 같은 자리**(`auto_candidate`)에 쌓아 페이지 상세 '수정필요' 칸에 함께 보인다(카드에 `값 대조` 이름표, 개발 화면 위 번호 핀).
두 검수는 `auto_run.source`('engine' / 'value')로 갈라 둔다 — 그림 검수 회차를 찾을 때 서로 섞이지 않는다.
읽는 자료는 촬영기가 접수 때 넣어 둔 `uploads/{page}_design.json` + `{page}_dev값.json` 이고, 값은 **마지막 차수**의 개발 화면을 잰 것이라
옛 차수에는 붙이지 않는다. 자료가 바뀌면(두 파일 지문) 새 회차로 다시 재되 사람이 내린 판정은 **이어받는다**(옛 회차는 남는다).
사람이 '제외'로 내리면 **수정요청서에서도 빠진다** — 문서는 데이터를 읽어 만드는 출력물이다(2번-1). 판정은 `rule_verdict`에 규칙 이름 `value`로 쌓인다.
값 대조 후보의 판정은 요소 층 정책으로 쌓지 않는다(그림 검수 규칙이 아니다). 그림 시안이 안 붙은 페이지도 값 대조만으로 후보를 본다.

**후보 카드 정리 + 로고는 그림 한 덩어리 (2026-09-14 완료, `mvp0/card_view.py` · `engine/ui.html` 그림표):**
카드 아래에 회색 글줄로 붙어 있던 것을 **[무엇이 · 지금 개발 · 디자인 기준] 표 + '개발이 볼 자리'** 로 갈랐다.
같은 값을 두 번 적지 않는다(옛 '디자인 원본값' 줄 없앰). 값 대조 후보는 `auto_candidate.policy` 에 줄·자리를
**값째로** 남긴다(글월 되읽기 없음) — 카드 모양을 바꿀 땐 `value_candidates.지문()` 앞머리를 올려 새 회차로 다시 잰다.
기준값에 **토큰·컴포넌트 이름**이 있으면 그 이름을 함께 보인다. 토큰은 시안 플러그인(`plugin-pick/code.js` `요소토큰`)이
변수·스타일에서 읽어 요소마다 `토큰` 칸으로 싣고, 레거시 화면은 비어 있어 헥사만 보인다(river 2026-09-14).
**그림표(POLICY_IMAGE_RULES · river 확정 · 모든 서비스 공통):** 이름이 로고·이미지를 가리키고 글자가 곁들임뿐인
작은 덩어리는 안쪽 글자를 글자로 보지 않고 **그림 한 덩어리**로 견준다. 크기(잉크 가로폭)는 **로고 덩어리에서만** 잰다 —
작은 아이콘까지 재면 잉크 넓히기가 옆 요소를 물어 10화면 볼 것이 +35건 늘었다(실측). 문턱 8%·3px.
회귀(2026-09-14): 10화면 볼 것 302 그대로 · 체류시간 정답 6/7 그대로 · 자체검사 214 통과.
(**주의 · 2026-09-15 고침:** 그때까지의 10화면 숫자는 `run.js` 가 정렬을 ty=0 으로 고정한 채 잰 값이다 —
`열개.sh` 가 `FORCE_TY=""` 로 넘기는 것을 '준 것'으로 보았다. 정렬을 살려 다시 재면 **볼 것 316 · 체류시간 정답 7/7**.)

**세로로 긴 화면은 큰 덩어리마다 따로 맞춘다 (2026-09-15 완료, `engine/ui.html` `bandAlignment`):**
세로로 긴 화면은 구역마다 밀린 정도가 달라, 화면을 통째로 한 번 밀면 위나 아래 한쪽이 틀어지고
그 자리의 글자·아이콘이 엉뚱한 곳을 봐 **'디자인에 있는데 개발에 없다'** 가 줄줄이 올라온다.
기준 요소 표에 그 요소가 디자인 어디쯤인지(`dy`)를 함께 싣고, 맨 바깥 층 덩어리마다 세로 밀림을 따로 정한다.
**함부로 흔들지 않는다** — 표가 둘 이상 · 구간 안에서 말이 갈리지 않음(뭉치는 무리가 구간 표의 ⅓ 이상) ·
그 구간 그림 윤곽이 실제로 더 잘 겹침(+0.06), 셋을 다 넘겨야 바꾼다. 전체 값과 화면 높이의 10%를 넘게 벌어지면 그대로 둔다.
좌표는 `tyAt()` · `drawAligned()` 한 통로로 모았다(캡처를 얹는 세 곳 · 되돌리는 일곱 곳).
회귀: **화면 14개**(기존 10 + 세로로 긴 홈 4) 볼 것 461 → 457, 홈_로그인 시 37 → 33(줄어든 4건이 그 헛지적), 나머지 13개 그대로.
체류시간 정답 7/7 · 자체검사 222 통과. 재는 법: `validation-image-qa/repro/잰다.sh <이름> [ui.html]` → `node 열넷세기.js <새> <옛>`.
**시안을 전체 길이로 통일하는 것은 답이 아니었다**(river 2026-09-15 시험) — 같은 화면을 잘라 그린 쪽이 오히려 잘 맞았다.
아래로 갈수록 어긋나는 게 원인이라, 시안을 길게 그리면 그 구간까지 끌어안는다.

**찍을 목록 동작 칸 자동 채움 (2026-09-15 완료, main 합침):** 촬영 준비 ②에서 동작을 이름으로 짓고(띄어쓰기 무시·바탕 화면 단추 우선),
못 지으면 시안 두 장을 견줘 **새로 생긴 덩어리 바로 위의 것**을 짚고(근거 표시), 그래도 없으면 **그림 위에서 눌러 고른다**.
요소는 플러그인이 포털용으로 이미 보낸 12단계 전체 목록(`site/시안요소.py`)에서 읽는다(다시 읽지 않음).
자료·계정 조건 화면(1개 등록·운행 종료·게스트…)은 눌러서 못 만든다고 표시만 한다(삼성통근버스 시안 402장 중 4할). 피그마 프로토타입 읽기는 안 한다(시안 작업이 늘어남 — river).
찍는 손은 개발 글자가 시안과 살짝 달라도 가장 닮은 것(72%↑)을 누르고 기록에 남긴다. 홈 4장 실제 촬영 4/4 · 포털 접수 확인. 정본: `capture-app/규칙.md` 7장.

**자 하나로 — 좌표를 한 곳에서만 바꾼다 (2026-09-16 첫 조각, `자.py`):**
자가 다섯이라 새 기능마다 하나를 잊었다. 기준 자를 **화면 기준 px**(시안 한 판의 좌표계) 하나로 두고,
`자.py` 가 길 넷(잰값→화면 / 화면→개발그림 / 화면→시안그림 / 되돌리기)만 연다. 브라우저도 `자.JS()` 로 **같은 셈**을 쓴다.
옮긴 곳: `valueqa/match.py`(짝맞추기가 개발 상자를 시안 자 위로 옮겨 놓고 셈한다) · `valueqa/candidates.py`(너비·높이도 같은 자로) ·
`mvp0/value_candidates.py`(핀 자리) · `mvp0/comparison_view.py`(나란히 보기 여백·잘라 오는 넓이).
이때 드러난 것: 홈 4장은 **판 폭(5120)** 을 화면 폭으로 잘못 보아 '폭 다름'이 켜져 모든 차이가 참고로 내려가 있었다 —
이제 제대로 견준다(볼 것 256 → 262 · 짝 369 → 358, 늘어난 것은 그동안 덮여 있던 실제 차이다).
**남은 곳까지 옮김 (2026-09-16):** `mvp0/auto_inspect.py` — 후보를 잰 촬영본 → 지금 쓰는 좌표 기준으로 옮기던
자리 둘(지적 등록 때의 상자 · 화면에 넘기는 배율)이 **가로·세로에 각각 다른 배율**을 쓰고 있었다.
이제 `자.그림자()` 하나로 옮기고, 비가 갈리면 점검이 짚는다(지금 자료는 세 묶음 다 1.000 배로 같다).
`design_plan` · `intake_store` · `design_receive` · `capture-app/site/intake.py` 는 **옮길 것이 없었다** —
그림의 가로·세로를 칸에 적어 넣기만 하고 좌표를 바꾸지 않는다(이름만 보고 센 것이었다).
**남겨 둔 곳 하나:** `engine/ui.html` 의 정렬(`bandAlignment`·`tyAt`). 거기는 **자를 찾아내는** 일이라
(이미 아는 두 자 사이를 옮기는 것이 아니다) 이 자에 넣지 않는다. 옮기려면 화면 14개 회귀를 함께 재야 한다.
점검: `python3 scripts/자점검.py` — 자 자체검사 + 회차마다 촬영본·좌표 기준의 비 + 접수 화면 26장이 같은 자 위에 있는지.

**시안 얹을 자리가 겉돌면 세로만 다시 찾는다 (2026-09-16, `engine/ui.html` `verticalRescue`):**
기준 요소가 두어 개뿐이고 그 둘이 **같은 줄**에 있으면 함께 틀려도 서로를 걸러 주지 못한다 —
앱 로그인 화면에서 화면 절반이 통째로 밀려(ty 175, 정답 27) 아이디 찾기·비밀번호 찾기 핀이 로고를 찍었다.
덩어리별 맞춤(`bandAlignment`)은 한 덩어리에 근거가 둘 이상이어야 켜져 이 화면에선 아예 안 돌았다.
그래서 **맨 마지막에 한 번 더 본다** — 크기·가로는 그대로 두고(캡처 폭이 정답이다) 세로만 훑어,
눈에 띄게(+0.05) 더 잘 겹치는 자리가 있으면 그리로 옮긴다.
셈하는 자리는 **검수하는 구간만**이다(위·아래로 떼어낸 띠를 뺀 `liveBand`). 빈 곳끼리 겹쳐 점수가 뜨는 것을 막으려고
캡처가 그 구간의 8할을 덮는 자리만 센다 — 이 울타리가 없으면 시안이 화면 밖으로 나간 자리가 1등이 된다(실측).
회귀: 기존 화면 14개 볼 것 408 그대로 · 체류시간 정답 7/7 · 자체검사 222 통과.
그 앱 화면은 볼 것 6 → 2(사라진 4건이 그 헛지적). **회귀 목록은 이제 15개** — `열넷세기.js` 에 `appkbd` 를 더했다.
남은 것: 자리를 확신 못 할 때 **화면에 한 줄 알리기**(지금 자신감 점수는 이 화면에서 0.78로 높아 쓸 수 없다 —
겹침 정도·자리를 다시 잡았는지로 걸어야 한다).

**다음 작업 대기열 (우선순위 순):**
0-3. ~~검수 페이지를 별도 화면으로 나누기~~ — **완료(2026-09-15, `mvp0/page_move.py`).** 상세는 `docs/HANDOFF_화면분리.md`.
   검수 페이지 목록에서 페이지를 골라 **'고른 페이지 옮기기'** 한 단추로 두 갈래 — 새 화면으로 옮기거나 이미 있는 화면에 합친다(되돌리기도 같은 단추).
   새 사람키는 같은 접두의 **비어 있는 다음 번호**를, 화면 이름은 **페이지 이름의 공통 뿌리**(`웹_홈_…` 4장 → `홈`)를 제안하고 사람이 고친다.
   자동으로 나누지 않는다 — 제안만 하고 사람이 누른다(2번-2).
   지우는 것은 없다: `inspection_page`·`inspection_run`·`inspection_issue` 의 `screen_id` 만 바뀌고,
   page_id 로 매달린 것(후보·이력·옛 회차·시안 판)은 그대로 따라온다. 옮긴 사실은 `page_move_event`(page_id, from_screen, to_screen, actor, at, note)에 append-only(고침·지움 잠금).
   `rule_verdict` 는 손대지 않는다 — 거기 적힌 화면은 '판정하던 그때의 사실'이라, 규칙 성적표(`rule_log`)가 `page_id` 로 지금 화면을 되짚는다.
   요소 층 `policy_rule` 은 **옮기는 페이지의 시안에 있는 요소만**, 화면 층은 **새 화면일 때만** 받는 화면에 한 벌 더 쌓아 이어받는다
   (사람이 내린 제외·가변 판정이 화면 uuid 아래 있기 때문, 8번). 다른 과제(프로젝트)의 화면으로는 못 옮긴다.
   남는 것: 되돌려서 빈 화면이 되면 그 화면은 목록에 남는다(포털에 화면 지우기가 없다).
0. **검수 정책 규칙화 — 1차 완료(2026-09-09, 볼 것 647→329·정답 7/7). 남은 것: #1 표 항목 이름 고정(근거 화면 부족) · 앱 화면 규칙 실전 검증(우비스 버스 다른 화면).**
   정렬을 고친 뒤 드러난 **다음 병목**. 8번 3계층 정책의 첫 실물 조각이다. 범위:
   ① 규칙을 코드에 흩어두지 말고 **한 곳(정책 파일/표)** 에 모은다
   ② 기본 규칙 4줄 — 표 **컬럼 제목 고정** / **메뉴 이름 고정** / 표 본문 값은 가변 /
      디자인에 그려 넣은 **가짜 브라우저 틀·작업표시줄 제외**
   ③ 체류시간 관리 화면에서 **확정 170건·사람이 볼 93건이 몇 건으로 줄고 정답 8건이 다 남는지** 확인.
   금지: 자동 확정, 화면별로 사람이 매번 설정하게 만들기(3계층 상속의 기본값으로 흡수한다).
   ③ 판정↔규칙 연결 기록 — **완료(2026-09-14)**. 8번 참조. ④⑤(규칙 발굴·제안 숫자)가 남았다.
0-1. **글자 비교 감도 — 짧은 글자 바뀜을 놓치는 자리** (2026-09-14 추가, 0번의 남은 조각 #1과 한 몸).
   표 목업의 심은 오류 3개 중 컬럼 제목 `연락처 → 담당자 휴대전화번호`를 못 잡는다(재현 확인).
   원인 둘: ① 비교를 **디자인 글자 상자 안에서만** 해서 상자 밖으로 길어진 글자가 안 보인다
   ② 짧은 글자에서 `ncc` 문턱이 헐겁다(글꼴 폴백 때문에 일괄 상향은 금지).
   방안·검증 기준은 `validation-image-qa/repro/HANDOFF_text_sensitivity.md`.
   금지: 배포 엔진에 글자 읽기(OCR·로컬 AI) 넣기, 화면별 수동 문턱, 자동 확정.
0-2. **촬영 앞 '시안 점검' 한 장** (river 확정 2026-09-14 — 다음 조각).
   검수를 페이지 상세까지 끌고 가기 전에, **시안이 정돈됐는지 먼저 본다.** 자리는 촬영 준비 사이트의 **접수 직전**.
   프레임을 골라 보내면 점검 결과 한 장을 보여주고, 사람이 보고 넘긴 것만 촬영으로 간다(막지 않는다 — 2번-2).
   시안만으로 보는 것 셋:
   ① **규정 대조** — 토큰·컴포넌트 준수. 조각 ②(`value-qa` 규정 대조)를 **시안 쪽에만** 돌린다. 새 자(尺)를 만들지 않는다.
   ② **상태 빠짐** — 촬영 대본(`capture-app/apps/*.yaml`)에 나오는 화면 상태에 해당하는 시안 프레임이 있는지 맞대본다.
      (2026-09-14 실제 사례: '비밀번호 숨김 해제 상태' 시안이 없어 못 찍음)
   ③ **이름 규칙** — 프레임 이름·화면 사람키(7번 규칙) 어긋남.
   시안만으로는 **못 보는 것**(글자 넘침·실제 창 높이에서 깨짐)은 그대로 페이지 상세에 남긴다.
   금지: 점검 미통과를 이유로 촬영을 자동 차단, 시안을 자동 수정, 화면별 수동 설정 요구.
1. ~~`plugin-image-qa/` 1.0 나머지~~ — **폐기(2026-09-14).** 피그마 검수기를 쓰지 않기로 해 이 조각은 하지 않는다.
   필요한 것(번호 패널·디자인 원본값 표시)은 포털 페이지 상세에서 다시 판단한다.
2. **차수 연동 2차 조각** — 새 차수 추가 + 이전 지적 끌고와 판단
   (고쳐짐=해결후보 / 안 고쳐짐=미해결 유지 / 신규 추가). **사람이 판단, 자동비교 아님.**
   지금은 데모 시드된 1·2차를 '보기'만 되고, 실제로 새 차수를 만드는 흐름이 없다.
3. **실제 검수 데이터 더 넣기** — 지금 한 화면(15건)뿐. 여러 화면·페이지로 탭·카운트·핀 검증.
4. **A4 반출 본구현** — 현재 버튼만 스텁(`report.py` 미변경).
5. **오류 수정방법 값 채우기** (목표값·디자인 토큰).
6. **차수 비교 뷰**(1차↔2차 나란히), **포털에서 직접 네모 치기**, **버전 확정·발행**(17번 정책).
7. **(별도 설치)** 웹 자동 캡처 / 앱 캡처 가이드(20번 방침) / 자동 비교.

이 목록은 **대기열일 뿐**이다. 각 항목은 착수 전에 범위를 확인받고, 4번 가드레일을 우선한다.
