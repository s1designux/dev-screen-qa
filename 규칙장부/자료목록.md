# 자료 목록 — 무엇이 정본인가 (①단계)

숫자는 사람이 적지 않는다. `규칙장부/도구/자료점검.py` 가 재서 `규칙장부/자료/자료점검-최근.json` 에 적고,
이 문서에는 **사람이 확정한 것**(정본 · 격리 · 치우는 조건)만 적는다.

다시 재는 법 (자료가 저장소 밖에 있으므로 자리를 알려 준다):

```
python3 규칙장부/도구/자료점검.py --자료뿌리 ~/dev-screen-qa --포털시험
```

마지막으로 잰 때: **2026-09-16 12:37** (그때의 숫자는 위 json).

---

## 1. 정본 (확정)

| 무엇 | 자리 | 지문(sha256 앞머리) | 확인 |
|---|---|---|---|
| 검수 자료함 | `~/dev-screen-qa/mvp0/mvp0-real.db` | `c559c6837a435d6e…` (2026-09-16 11:33, 8.6MB) | 무결성 ok · 되살아남 |
| 그림·자료 방 | `~/dev-screen-qa/mvp0/uploads` (469개) | — | 자료함이 가리킨 302개가 모두 있다(없는 것 0) |

정본으로 고른 근거는 날짜가 아니라 **대조**다. 다른 사본 17벌을 판정·이력 표(지적·이력·후보·후보 판정·규칙 성적·정책·접수 이력)의
열쇠로 맞대 보니, **사람이 내린 판정은 한 건도 정본 밖에 있지 않다**. 아래 세 벌만 정본에 없는 행을 갖고 있고, 그 셋은 성격이 다르다.

자료함·그림방은 **깃에 없다**. 저장소를 다시 받아도 따라오지 않는다 — 백업 폴더가 유일한 사본이다.

## 2. 정본에 없는 것을 가진 사본 (치우지 않는다)

| 자리 | 정본에 없는 것 | 무엇인지 | 치우는 조건 |
|---|---|---|---|
| `mvp0/mvp0.db` | 지적 4 · 이력 11 | 초기 합성 픽스처(CV-WEB-012 라이브 모니터링). 실제 검수가 아니라 데이터 모델 검증용 | 시험 자료로 계속 쓴다. 치우지 않는다 |
| `mvp0/mvp0-intake-preview.db` | 접수 이력 13 | 접수함 미리보기 시험 자료(2026-09-09) | 촬영 접수 흐름을 다시 시험할 일이 없다고 river가 정하면 |
| `mvp0/mvp0-real.db.bak-범위조정전-20260910-1610` | 후보 85 | 다시 만든 앱 로그인 화면(BUS-AND-001)의 **옛 엔진 후보**. 사람 판정(후보 판정·규칙 성적)은 0건 | 그 화면 후보를 다시 볼 일이 없다고 정하면 |

## 3. 되살아나는 것을 확인한 사본 (당장은 그대로 둔다)

- 작업 이름별 `.bak` 11개 (`mvp0/mvp0-real.db.bak-*`) — 전부 무결성 ok · 표 읽힘. 정본에 없는 판정·이력은 위 한 벌 말고는 없다.
- 하루 백업 3벌 (`~/dev-screen-qa-백업/2026091*-*/mvp0-real.db`) — 전부 되살아남. 하루 한 벌·7일치만 남는다.
- 빈 파일 2개: `mvp0/mvp0-demo.db`, `~/dev-screen-qa/mvp0-real.db`(뿌리). 0바이트라 되살릴 것이 없다.
  데모 포털은 `MVP0_DB` 로 따로 가리켜 쓴다.

치우기는 **river 승인 뒤에만** 한다. 그 전 단계로 프로그램이 두 가지를 확인한 것만 치울 후보가 된다 —
되살아나는지 · 정본에 없는 판정·이력이 없는지.

## 4. 되살리기 시험 (백업이 진짜 살아나는가)

하루 백업 최신 한 벌(`20260915-1830`)을 임시 자리에 되살려 포털을 임시 포트로 띄워 봤다.
화면 목록 · 화면 · 페이지 상세 · 그림이 모두 200으로 열렸다(그림 38KB). 정본과 그림방은 건드리지 않았다.
`--포털시험` 을 붙이면 프로그램이 이 시험을 처음부터 다시 한다.

## 5. 아직 못 믿는 것 (정직하게 남김)

- 그림방 469개 가운데 **167개는 자료함이 가리키지 않는다**. 옛 차수·시험 그림으로 보이지만 한 장씩 확인하지 않았다 — 지우지 않는다.
- 백업은 자료함과 그림방만 담는다. 재현 세트(정답·시안·개발 그림)는 **여기에 안 들어 있다** — ②단계 시험지 분리에서 다룬다.
- 백업은 하루 한 벌·7일치만 남는다. 지금 남은 것은 3벌(09-14 두 벌·09-15 한 벌)뿐이고, 09-16 백업은 아직 돌 시각 전이다.
