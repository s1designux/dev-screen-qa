# 기준 성적 (출발점)

앞으로의 모든 비교는 여기서 시작한다. 숫자는 `규칙장부/도구/공식측정.py` 가 잰 것이다.

| | |
|---|---|
| 측정번호 | `001-20260916-1240` (같은 조건으로 한 번 더 재서 같은 숫자 확인: `002-20260916-1243`) |
| 검수기 | 이 가지(`feat/worker-structure`) HEAD 의 `engine/ui.html` — 지문 `543fd19bd1fd…` |
| 시험지 | 판 `2026-09-16` · 화면 15개 · 정답 7건 |
| 판정 | **잼** — 열다섯 화면 다 쟀고, 정답 7/7 살았고, 자체검사 222개 실패 0 |
| 볼 것 합계 | **414** |

화면별 볼 것: board 51 · vehicle 27 · dash 13 · door 19 · stay 56 · findid 10 · login 5 ·
table 3 · codes 3 · route 92 · home1 33 · home2 36 · home3 29 · home4 31 · appkbd 6

## 이 숫자를 옛 기록과 나란히 놓지 않는 이유

지난 기록의 **457**(화면 14개)과 여기 **414** 는 견줄 수 없다. 재는 조건이 다르다 —
앱 키보드 화면(appkbd)이 시험지에 새로 들어왔고, 그동안 검수기도 바뀌었다.
앞으로는 검수기 지문과 시험지 지문이 같은 성적끼리만 견준다.

## 함께 적어 두는 사실

- 본 폴더(`~/dev-screen-qa`)에는 **아직 커밋되지 않은 검수기 수정**이 있다(세로 정렬을 윤곽으로 다시 보는 조각).
  기준 성적은 그것을 **넣지 않은** 판으로 쟀다. 그 조각이 커밋되면 같은 시험지로 다시 재서 나란히 놓는다.
- 재현성: 같은 검수기·같은 시험지로 두 번 재서 화면별 숫자까지 모두 같았다.
