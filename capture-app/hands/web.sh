#!/bin/bash
# '찍는 손' — 웹 자리. 아직 비어 있다.
# 촬영기의 나머지(이름표·이름 규칙·결과 폴더·실행 버튼)는 웹에서도 그대로 쓴다.
# 웹은 브라우저를 다루는 도구가 따로 필요해서, 그 조각에서 이 파일만 채운다.
echo "웹 촬영은 아직 만들지 않았습니다. 이름표의 '플랫폼'을 android로 두고 써 주세요." >&2
exit 2
