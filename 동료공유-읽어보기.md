# 동료와 함께 쓰기 (내부망 PC에 포털 올리기)

## 1. 내부망 PC에 옮기기
1. 깃허브에서 이 저장소를 내려받는다.
2. 검수 자료는 깃허브에 없다. 외부망 PC의 `mvp0/mvp0-real.db`와 `mvp0/uploads/` 폴더를
   내부망 PC의 같은 자리에 따로 넘긴다.

## 2. 포털 켜기
- 윈도우: `포털-켜기-윈도우.bat` 두 번 누르기
- 맥: `포털-동료공유-켜기.command` 두 번 누르기

검은 창이 뜨고 동료에게 줄 주소가 나온다. **창을 닫으면 포털이 꺼진다.**
처음 켤 때 방화벽이 물어보면 허용한다.

동료는 그 주소를 브라우저에 치면 들어온다. 같은 망 안에서만 열리고,
망 밖에서 오는 접속은 포털이 막는다.

혼자 쓸 때는 그냥 `python portal.py`로 켜면 예전처럼 그 PC에서만 열린다.

## 3. 자동 백업 걸기 (한 번만)
- 윈도우: `백업-걸기-윈도우.bat` 두 번 누르기
- 맥: 이미 걸려 있음 (매일 18:30)

검수 자료와 이미지를 `사용자폴더/dev-screen-qa-백업`에 복사하고 7일치를 남긴다.
원본은 건드리지 않는다.

## 4. 주의
- 한 번에 한 사람씩 처리하는 구조다. 여럿이 동시에 누르면 잠깐씩 기다린다.
- 같은 페이지를 두 사람이 동시에 고치면 한쪽이 밀릴 수 있다. 자리를 나눠 쓴다.
- 포털 PC가 잠자기로 들어가면 동료 화면이 안 열린다. 절전 설정을 꺼둔다.
