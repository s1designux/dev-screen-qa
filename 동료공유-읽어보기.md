# 내부망 PC에 검수 포털 올리기 (윈도우 기준)

한 번만 하면 된다. 순서대로 따라간다.

## 1단계 — 파일 내려받기
깃허브 저장소 페이지 → 초록색 `Code` 단추 → `Download ZIP`.
받은 압축을 풀어 `C:\dev-screen-qa` 에 둔다. (경로에 한글·띄어쓰기가 없는 게 안전하다)

## 2단계 — 검수 자료 넣기
검수 데이터와 이미지는 깃허브에 없다. 따로 받은 것을 아래 자리에 그대로 넣는다.

- `mvp0-real.db` → `C:\dev-screen-qa\mvp0\mvp0-real.db`
- `uploads` 폴더 → `C:\dev-screen-qa\mvp0\uploads\`

이 단계를 건너뛰면 포털이 "mvp0-real.db 없음" 이라며 안 켜진다.

## 3단계 — 파이썬 확인
시작 → `cmd` → 검은 창에 다음을 친다.

    python --version

버전 숫자가 나오면 된다. `3.9` 이상이면 충분하다.

## 4단계 — 포털 켜기
`포털-켜기-윈도우.bat` 을 두 번 누른다.

- 윈도우 방화벽이 물어보면 **허용**을 누른다. (한 번만)
- 검은 창에 동료에게 줄 주소가 나온다. 예: `http://10.20.30.40:8765`
- **이 창은 닫지 않는다.** 닫으면 포털이 꺼지고 동료 화면도 안 열린다.

## 5단계 — 확인하고 주소 알려주기
그 PC 브라우저에 나온 주소를 쳐서 화면 목록이 보이는지 본다.
보이면 동료에게 그 주소를 알려준다. 동료는 브라우저에 치기만 하면 된다.

같은 망 안에서만 열리고, 망 밖에서 오는 접속은 포털이 막는다.

## 6단계 — 자동 백업 걸기
`백업-걸기-윈도우.bat` 을 두 번 누른다. 한 번만 하면 된다.

매일 저녁 6시 30분에 검수 자료와 이미지를 `사용자폴더\dev-screen-qa-백업` 에 복사하고
7일치를 남긴다. 원본은 건드리지 않는다.

## 7단계 — 절전 끄기
설정 → 시스템 → 전원 → **화면·절전 모드를 "안 함"** 으로.
PC가 잠자기로 들어가면 동료 화면이 안 열린다.

---

## 나중에 기능이 바뀌면
외부망에서 고쳐 깃허브에 올린 뒤, 내부망 PC에서 **1단계만** 다시 한다.
검수 자료(`mvp0-real.db`·`uploads`)는 덮어쓰지 않는다 — 그게 그동안 쌓인 검수 결과다.

## 알아둘 것
- 한 번에 한 사람씩 처리하는 구조다. 여럿이 동시에 누르면 잠깐씩 기다린다.
- 같은 페이지를 두 사람이 동시에 고치면 한쪽이 밀릴 수 있다. 자리를 나눠 쓴다.
- 혼자 쓸 때는 `mvp0` 폴더에서 `python portal.py` 로 켜면 그 PC에서만 열린다.

## 맥에서 켤 때
- 포털: `포털-동료공유-켜기.command`
- 백업: `백업-지금-맥.command` (자동 등록은 launchd 사용)
