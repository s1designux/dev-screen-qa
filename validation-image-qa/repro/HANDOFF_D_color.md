# D 조각 — ③ 캡처 색 치우침 무시 · ④ 색 판정이 어긋난 자리에서 나오지 않게

**여기서 일한다:** `/Users/designgroup_02/dev-screen-qa-color` · 브랜치 `feat/image-qa-color-profile`
검수 자료(PNG·elements JSON·기준선 결과 `screen_*.json`)를 이 폴더에 **복사해 두었다.** 여기서 바로 잰다.

## 왜 하는가 (river님 확인, 2026-09-09)
「색상이 다르게 보입니다」 49건을 눈으로 본 결과:
- 일부는 **캡처를 저장한 상태(RGB/CMYK 등)** 때문에 색이 달라 보이는 것이다 → 오류가 아니다.
- **21번부터는 색 문제가 아니라 자리(위치)가 어긋난 것**이다 → 색이라고 말하면 안 된다.
(같은 확인에서 나온 게시판 전용 패스 2가지 — 목록 번호 칸의 숫자↔알림아이콘, 신규 글 제목 끝 아이콘 —
 은 **근거 화면이 하나뿐이라 이 조각에서 하지 않는다.** 게시판 화면이 하나 더 생기면 그때 만든다.)

## 할 일
1. **③ 색 치우침 보정:** 화면 전체가 같은 방향으로 조금 치우친 색 차이는 무시한다.
   (디자인·개발 두 이미지의 전체 색 치우침을 재서 빼고 나서 비교. 치우침이 크면 "캡처 색이 전체적으로 다릅니다" 한 줄로만 알린다.)
2. **④ 어긋난 자리에서는 색을 말하지 않는다:** 그 요소를 개발 캡처에서 제대로 못 찾았거나
   자리가 어긋난 경우 색 판정을 내지 않는다(이미 있는 「못 찾은 자리에선 입 다물기」 규칙과 같은 결).
3. 화면 10개로 재서 **볼 것 · 색 건수 · 체류시간 정답**을 기준선과 비교하고, 규칙 기록을 남긴다.
4. 커밋 전 멈추고 river님께 보고(표 + 잘라낸 그림).

## 기준선 (커밋 940a994 기준, 이 폴더의 `screen_*.json`)
볼 것 **331** · 그중 색 **64** · 체류시간 정답 6/7(로고 워드마크 1건은 원래 접혀 있는 별건).
화면별 색: 체류시간 21 · 게시판 20 · 노선관리 13 · 차량위치 8 · 표 1 · 코드 1.

## 재는 법
```bash
cd /Users/designgroup_02/dev-screen-qa-color/validation-image-qa/repro
zsh runseq.sh <ui.html 경로> <결과이름>     # 화면 10개 순서대로
node colorcount.js <결과이름>               # 볼 것·색 건수
node answers.js  <결과이름>                 # 체류시간 정답 생존
```
- `color_judge.html` = river님이 본 색 후보 49건 판정지.
- **노선관리(route)는 정렬을 손으로 고정한 화면**이라 근거로 쓰지 않는다.
- 크롬이 가끔 멈춘다. 한 화면이 2분 넘게 안 끝나면 `pkill -f "headless=new"` 후 그 화면만 다시 돌린다.
- 앞서 실패한 시도 2가지(다시 하지 말 것): 진한 획 5% 중앙값(정답이 밀림) · 색상(hue) 중심 비교(노선관리 13→50건).

## 지켜야 할 것
`git add .` 금지 · 회사 화면 자료와 산출물은 커밋하지 않는다 · 커밋 전 멈추고 보고.
