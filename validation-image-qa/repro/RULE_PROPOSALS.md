# 검수 규칙 제안 대기 목록

로컬 AI가 화면을 읽고 → **코드가 어긋난 것을 세어** 제안하고 → river님이 승인한 것만 규칙이 된다.
AI에게 "어떤 규칙이 필요하냐"고 묻지 않는다. 세는 것은 코드, 정하는 것은 사람.

상태: `제안` → `보류` / `승인`(규칙 반영) / `거절`(사유 기록, 다시 제안하지 않음)

---

## #1 표의 항목 이름(라벨)은 고정으로 본다

| | |
|---|---|
| **상태** | **보류** (2026-09-08, river) — "다른 화면을 더 보고 정하자" |
| 처음 제안 | 2026-09-08 |
| 근거 화면 | 노선관리_add (실제 회사 화면, PC 1920×1080) — **1개** |

**문제:** 표의 항목 이름 7개가 '데이터가 들어가는 자리'로 잘못 분류돼 사람 눈에 안 보인다.
> 노선명 · 노선 구분 · 운영 상태 · 출발지 · 노선 번호 · 노선 경로 · 운행거리 구분

**원인:** 이 항목들을 담은 피그마 프레임 이름이 `dataview`. 정책의 '컴포넌트 역할 이름' 규칙이
이름에 `data`가 들어가면 데이터 자리로 본다(`ROLE_VAR_RE`). 요소 자신의 이름은 `항목`인데,
`항목`이 고정 낱말 목록(`ROLE_FIXED_RE`)에 없어서 부모 이름에 밀린다.

**접혀서 안 보인 것 중 진짜 차이 3건 (로컬 AI가 개발 캡처에서 읽어 확인):**

| 디자인 | 개발화면 | 종류 |
|---|---|---|
| 노선 구분 | 노선구분 | 띄어쓰기 빠짐 |
| 운행거리 구분 | 운행거리구분 | 띄어쓰기 빠짐 |
| 운영 상태 | 노선 상태 | 글자가 바뀜 |

**고치는 방법:** `plugin-image-qa/ui.html`의 `ROLE_FIXED_RE` 맨 앞에 `항목|` 한 낱말 추가.
(요소 자신의 이름이 부모 이름보다 먼저 판정되므로 이 한 줄이면 된다.)

**켰을 때 효과 (계측 완료 — 원본은 안 건드리고 사본으로 측정):**

| 화면 | 지금 (후보/접힘/볼것) | 규칙 넣으면 | 결과 |
|---|---|---|---|
| 노선관리 | 448 / 222 / 226 | 448 / 215 / 233 | 숨어 있던 **7건이 보임** |
| 체류시간 | 239 / 75 / 164 | 239 / 75 / 164 | 변화 없음 |
| 아이디찾기 | 13 / 0 / 13 | 13 / 0 / 13 | 변화 없음 |
| 로그인 | 6 / 0 / 6 | 6 / 0 / 6 | 변화 없음 |
| 표 목업 | 12 / 9 / 3 | 12 / 9 / 3 | 변화 없음 |
| 코드관리 | 3 / 0 / 3 | 3 / 0 / 3 | 변화 없음 |

체류시간 **정답 8건**: 지금도, 규칙을 넣어도 **접혀서 사라지는 것 0건**.

**보류 사유:** 근거 화면이 1개뿐. 나머지 4개 화면(게시판·차량위치·대시보드·모두앱)에서
같은 패턴이 또 나오면 그때 다시 올린다.

**재현:**
```bash
cd validation-image-qa/repro
cp ../../plugin-image-qa/ui.html /tmp/ui_probe.html
# /tmp/ui_probe.html 의 ROLE_FIXED_RE 앞에 '항목|' 추가한 뒤
FORCE_TX=0 FORCE_TY=99 ELEMENTS_JSON=elements_route.json DESIGN_PNG=design_route_1920x1080.png \
DEV_PNG=dev_route_1920x1080.png DESIGN_MAX=4096 node run.js /tmp/ui_probe.html route-probe
```

---

## #2 (관찰 — 아직 제안 아님) 같은 띄어쓰기 오류가 두 화면에서 반복

「운영 관리」→「운영관리」가 **체류시간**과 **노선관리** 두 화면의 상단 메뉴에서 똑같이 나왔다.
공통 메뉴라서 개발 쪽에서 한 번 잘못 넣으면 모든 화면에 퍼진다.

이건 '규칙'이라기보다 **개발 쪽 실제 오류**다. 다만 규칙 후보로는
"띄어쓰기만 다른 것은 가변 판정보다 먼저 지적으로 올린다"가 될 수 있다.
아직 효과를 재지 않았다 → **숫자 없이 올리지 않는다.**

---

## #1 재측정 (2026-09-08, 화면 4개 추가 후)

**왜 다시 쟀나:** 위 표의 노선관리 숫자(448 / 222 / 226)는 **덜 뽑힌 요소 목록**으로 잰 것이었다.
플러그인 내보내기 버튼으로 다시 받아 대조하니 기존 정답본 1073줄이 새 파일 1359줄에 **하나도 빠짐없이 포함**되고
286줄이 더 있었다(어제 조각으로 받아올 때 마지막 조각이 잘린 것으로 보인다). 그래서 완전본으로 다시 쟀다.
또한 앞선 표의 '접힘' 칸은 고정 판정까지 섞어 센 것이라, 이번에는 **가변으로 접힌 것만** 센다.

**켰을 때 효과 (완전본 · 원본은 안 건드리고 /tmp 사본으로 측정):**

| 화면 | 지금 (후보/가변접힘/볼것) | 규칙 넣으면 | 결과 |
|---|---|---|---|
| **노선관리(완전본)** | 533 / 198 / 335 | 533 / **193** / **340** | 숨어 있던 **5건이 보임** |
| 체류시간 | 239 / 76 / 163 | 239 / 76 / 163 | 변화 없음 |
| 아이디찾기 | 13 / 0 / 13 | 13 / 0 / 13 | 변화 없음 |
| 로그인 | 6 / 0 / 6 | 6 / 0 / 6 | 변화 없음 |
| 표 목업 | 12 / 10 / 2 | 12 / 10 / 2 | 변화 없음 |
| 코드관리 | 3 / 1 / 2 | 3 / 1 / 2 | 변화 없음 |
| 게시판(신규) | 138 / 71 / 67 | 138 / 71 / 67 | 변화 없음 |
| 차량위치(신규) | 73 / 17 / 56 | 73 / 17 / 56 | 변화 없음 |
| 대시보드(신규) | 39 / 18 / 21 | 39 / 18 / 21 | 변화 없음 |
| 출입문(신규) | 21 / 9 / 12 | 21 / 9 / 12 | 변화 없음 |

**사라지는 것 없음.** 어느 화면에서도 후보가 줄지 않았고, 체류시간은 판정이 하나도 바뀌지 않아 **정답 8건도 그대로**다.
바뀌는 후보는 노선관리 6건뿐(가변→고정 5건, button:고정→role:고정 1건).

**새 화면 4개에서의 근거:** 게시판·차량위치·대시보드·출입문에는 `항목`이라는 이름의 표 라벨이 **없어서**
같은 패턴이 재현되지 않았다(모바일·게시판 화면이라 그런 표가 없다). 즉 **근거 화면은 여전히 노선관리 1개**다.
다만 규칙을 켜도 다른 9개 화면이 전혀 흔들리지 않는다는 것은 이번에 확인됐다.

**재현:**
```bash
cd validation-image-qa/repro
cp ../../plugin-image-qa/ui.html /tmp/ui_probe.html   # ROLE_FIXED_RE 앞에 '항목|' 추가
FORCE_TX=0 FORCE_TY=99 ELEMENTS_JSON=elements_route_plugin.json DESIGN_PNG=design_route_1920x1080.png \
DEV_PNG=dev_route_1920x1080.png DESIGN_MAX=4096 node run.js /tmp/ui_probe.html route-full-rule
```

---

## #2 (관찰 갱신 2026-09-08) 띄어쓰기 오류 — 실재하지만 **AI 읽기로는 못 믿는다**

로컬 AI가 새 화면 4개에서 띄어쓰기 차이 3건을 냈다. **개발 캡처를 직접 잘라 눈으로 확인**한 결과:

| 화면 | AI가 낸 것 | 실제 | 판정 |
|---|---|---|---|
| 게시판 | 「내 정보」→「내정보」 | 개발도 「내정보」 | ✅ **진짜 오류** |
| 게시판 | 「내 글보기」→「내글보기」 | 개발은 「내 글보기」(띄어져 있음) | ❌ AI 오독 |
| 대시보드 | 「전체 운행 현황」→「전체운행현황」 | 개발은 띄어져 있음 | ❌ AI 오독 |

즉 띄어쓰기 오류는 **실제로 존재하지만**(내정보 · 앞선 화면들의 「운영 관리」→「운영관리」),
**AI 읽기의 띄어쓰기 판독은 3건 중 1건만 맞았다.** 이 상태로는 "띄어쓰기만 다르면 먼저 올린다"는
규칙의 효과를 잴 근거가 못 된다. **숫자 없이 올리지 않는다** → 제안하지 않고 관찰로만 남긴다.
다음에 다룬다면 AI가 아니라 **검수기 쪽에서 글자 폭·글자 수로 재는 방법**을 먼저 만들어야 한다.

---

## #3 (확인 기록 2026-09-08) '가짜 브라우저 틀 제외' 규칙이 실제로 동작했다

이미 들어가 있는 기본 규칙이지만, 새 화면 3개에서 **사람이 아무 설정도 하지 않고** 자동으로 걸렸다.

| 화면 | 디자인 쪽 | 개발 캡처 쪽 |
|---|---|---|
| 차량위치 | 가짜 주소창 78px 제외 | 진짜 브라우저 띠 81px 제외 |
| 대시보드 | 가짜 주소창 78px 제외 | 진짜 브라우저 띠 82px 제외 |
| 출입문(앱) | 상태바 12px · 하단 버튼 띠 25px 제외 | 하단 내비 50px 제외 |

이 규칙이 없었으면 이 띠들이 통째로 '없어짐/다름' 후보로 쏟아졌을 자리다. **유지.**

---

## 참고 — 2026-09-08 새 화면 4개 성적

| 화면 | 겹치기 | 후보 | 가변접힘 | 볼것 | 못 봄 | 잘못 접힘 | 헛지적 |
|---|---|---|---|---|---|---|---|
| 게시판 (PC) | 0.87 ✅ | 138 | 71 | 67 | 8 | 0 | 4 |
| 차량위치 (모바일) | 0.93 ✅ | 73 | 17 | 56 | 5 | 4 | 2 |
| 대시보드 (모바일) | 0.89 △ | 39 | 18 | 21 | 5 | 2 | 0 |
| 출입문 (앱) | 0.77 ❌ | 21 | 9 | 12 | 12 | 0 | 0 |

- 대시보드는 위쪽은 맞으나 아래로 갈수록 20~25px 밀린다 → 아래쪽 결과는 신뢰도 낮음.
- 출입문은 겹치기 0.77(기준 0.85 미달)이고 **디자인 3줄 ↔ 개발 6줄로 내용 자체가 다른 짝**이라
  숫자를 근거로 쓰지 않는다. '못 봄 12건' 중 8건이 드롭다운 값 하나(에스원 본사)의 되풀이다.
- 차량위치 '잘못 접힘 4건'은 탭·칩의 숫자(`전체 9,999`→`전체 9`, 칩 `56`→`1`)로,
  실제로도 데이터라 **접은 게 맞아 보인다**. 기계적 세기의 한계이지 규칙이 틀렸다는 뜻이 아니다.

---

## #4 (확인 기록 2026-09-08) 기본 규칙 3줄이 실제로 일을 한다 — 규칙을 끄고 재봄

CLAUDE.md 21번 기본 규칙 4줄 중 아직 안 재본 3줄을, **규칙을 하나씩 꺼서** 화면 10개로 다시 돌려 효과를 쟀다.
원본(`plugin-image-qa/ui.html`)은 건드리지 않고 `/tmp` 사본 3개로 측정. 세는 기준은 **실제로 사람 눈에서 숨겨지는 것**
(후보의 `status`가 `variable`/`excluded`)로 통일했다.

| 화면 | 후보 | 지금(숨김/볼것) | 컬럼제목 끄면 | 메뉴 끄면 | 본문값 끄면 |
|---|---|---|---|---|---|
| 게시판 | 138 | 72 / 66 | 그대로 | 그대로 | 40 / **98** |
| 차량위치 | 73 | 11 / 62 | 그대로 | 그대로 | 그대로 |
| 대시보드 | 39 | 20 / 19 | 그대로 | 그대로 | 3 / **36** |
| 출입문(앱) | 21 | 8 / 13 | 그대로 | 그대로 | 1 / **20** |
| 체류시간 | 239 | 91 / 148 | 102 / **137** | 94 / **145** | 41 / **198** |
| 아이디찾기 | 13 | 0 / 13 | 그대로 | 그대로 | 그대로 |
| 로그인 | 6 | 0 / 6 | 그대로 | 그대로 | 그대로 |
| 표 목업 | 12 | 9 / 3 | 그대로 | 그대로 | 5 / **7** |
| 코드관리 | 3 | 0 / 3 | 그대로 | 그대로 | 그대로 |
| 노선관리 | 533 | 219 / 314 | 그대로 | 그대로 | 35 / **498** |

**정답 8건(river님 2026-09-04 확정)이 규칙을 껐을 때 살아남는지:**

| 규칙을 끄면 | 정답 생존 |
|---|---|
| 컬럼 제목 고정 ✕ | **4건 사라짐** — 입문날짜·입문시간·출문날짜·출문시간이 표 본문에 휩쓸려 '가변'으로 접힘 |
| 메뉴 이름 고정 ✕ | 8건 다 살아남음(판정만 `menu:고정`→`screen:가변`으로 바뀜). 대신 상단 메뉴 글자 8개(기준 정보·운영 관리·통계 현황·대시보드·운행 계획·휴일·코드관리·체류시간 관리)의 판정이 데이터 자리로 뒤집힌다 |
| 표 본문 값 가변 ✕ | 8건 다 살아남음. 대신 **사람이 볼 것이 폭증** — 노선관리 314→498, 체류시간 148→198, 게시판 66→98 |

**결론**

| 기본 규칙 | 판정 | 근거 |
|---|---|---|
| 표 **컬럼 제목** 고정 | ✅ 검증됨 — **없으면 정답 4건이 사라진다** | 체류시간(실제 화면) |
| **메뉴 이름** 고정 | ✅ **유지 승인**(2026-09-08, river) — 정답을 살리진 않지만 메뉴 글자 8개를 데이터 자리로 오해하는 걸 막는다 | 체류시간 |
| 표 **본문 값** 가변 | ✅ 검증됨 — 정답을 하나도 안 지우면서 볼 것을 화면당 20~37% 줄인다 | 화면 6개 |
| 가짜 브라우저 틀·작업표시줄 제외 | ✅ (#3에서 검증됨) | 3화면 |

**눈으로 보는 자료:** `rule_check_report.html` (규칙별로 달라지는 후보를 시안 위에 표시)

**재현:**
```bash
# ui.html 사본에서 POLICY_TEXT_RULES 의 해당 규칙 test 첫 줄에 return null; 을 넣고
# (본문값 규칙은 POLICY_LIST_RULES 의 rowValueMissing.apply 도 함께 끈다) 10개 화면을 돌린다.
ELEMENTS_JSON=elements_stay_full.json DESIGN_PNG=design_stay_1920x1080.png \
DEV_PNG=dev_stay_1920x1081.png DESIGN_MAX=4096 node run.js /tmp/ui_noheader.html m_stay_noheader
```
