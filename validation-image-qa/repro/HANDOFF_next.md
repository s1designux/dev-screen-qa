# [새 세션용] 검수 정책 규칙화 — 남은 기본 규칙 검증

## 0. 먼저 할 일
- 저장소 루트 `/Users/designgroup_02/dev-screen-qa` 의 `CLAUDE.md` 를 읽고 전제로 삼는다.
- 브랜치 `feat/image-qa-bottom-range` · 작업 폴더 `validation-image-qa/repro/`
- `RULE_PROPOSALS.md` 를 읽는다(제안 #1 승인 완료, #2 관찰만, #3 확인 기록).
- 마지막 커밋: `6e69bf7`

## 1. 이 조각에서 할 일 (한 줄)
CLAUDE.md 21번의 기본 규칙 4줄 중 **아직 실제 화면으로 검증 안 된 것**을 검증한다.

| 기본 규칙 | 상태 |
|---|---|
| 표 **컬럼 제목** 고정 | ❓ 미검증 (`tableHeader:고정` 판정은 나오는데 효과를 안 쟀다) |
| **메뉴 이름** 고정 | ❓ 미검증 (`menu:고정` 판정은 나온다) |
| 표 **본문 값**은 가변 | ❓ 미검증 |
| 가짜 브라우저 틀·작업표시줄 제외 | ✅ **검증됨** — 차량위치·대시보드·출입문 3화면에서 사람 설정 없이 자동으로 걸림 |
| (추가) 표 **항목 이름** 고정 | ✅ **승인·적용됨** (2026-09-08) |

## 2. 쓸 수 있는 자료 (이미 폴더에 있음 · 커밋 안 됨)
화면 9개가 바로 돌아간다. 요소 목록·PNG 파일 이름은 `README.md` 참조.
`board`(게시판) · `vehicle`(차량위치) · `dash`(대시보드) · `door`(출입문) ·
`stay`(체류시간) · `findid` · `login` · `table` · `codes` · `route`(노선관리, `FORCE_TY=99` 필요)

**주의:** `elements_route.json` 은 286줄이 빠진 옛 파일이다. **`elements_route_plugin.json`(완전본)을 쓴다.**

## 3. 재는 방법 (제안 #1과 같은 절차)
1. `cp ../../plugin-image-qa/ui.html /tmp/ui_probe.html` — **원본은 승인 전에 안 고친다**
2. 사본에서 규칙을 켜고/끄고 `node run.js <사본> <이름>` 으로 돌린다
3. 세는 기준: **가변으로 접힌 것만** 센다(고정 판정을 접힘에 섞지 말 것 — 이번에 틀렸던 부분)
   `sum(1 for c in candidates if c.get('policy') and '가변' in str(c['policy']))`
4. 9개 화면 회귀 + 체류시간 정답 8건 생존 확인
5. `RULE_PROPOSALS.md` 에 숫자와 함께 기록하고 **river님 승인 후에만** 원본 적용

## 4. 보고 방식 (2026-09-08 갱신 · 전역 CLAUDE.md 참조)
- 보고는 **① 결과(표·이미지) ② 결정할 것** 두 칸뿐. 진행 중계·혼자 고친 일·도구 이름·다음 단계 예고는 쓰지 않는다.
- **비교·검수는 말로 설명하지 말고 이미지나 HTML로** 만들어 바로 눈으로 판정하게 한다.

## 5. 알아둘 것
- 겹치기가 틀어진 화면의 결과는 헛것이다. `*.align.png` 를 **눈으로** 먼저 본다.
- 대시보드는 아래로 갈수록 20~25px 밀린다 → 아래쪽 결과 신뢰도 낮음.
- 출입문은 겹치기 0.77·디자인 3줄↔개발 6줄이라 **근거로 쓰지 않는다.**
- 로컬 AI 읽기는 화면당 7~20분, 한 번에 하나만 돌린다(Ollama가 하나뿐).
- AI의 **띄어쓰기 판독은 못 믿는다**(3건 중 1건만 맞았음). 반드시 캡처를 잘라 눈으로 확인한다.
- `git add .` 금지. 회사 화면 자료는 커밋하지 않는다. 커밋 전 멈추고 보고.
