# [새 세션용] 검수 일꾼 체계 — 인수인계 (고침 2026-09-17 밤 · 마지막)

## 0. 먼저 할 일
- 이 폴더 `~/dev-screen-qa-일꾼체계` 에서만 일한다. 가지 `feat/worker-structure`.
  본 폴더 `~/dev-screen-qa` 는 **항상 main** — 거기서 가지를 갈아타지 않는다.
- **이 가지는 나 혼자 쓰는 곳이 아니다.** 촬영기 쪽 다른 세션이 같은 가지에 커밋하고
  같은 작업 폴더의 `capture-app/` 를 고친다(2026-09-16에 실제로 겪었다).
  **커밋할 때 `git add .` 금지 — 내 파일만 골라 담는다.** 남의 변경이 보이면 그대로 둔다.
- `CLAUDE.md` 를 전제로 삼는다. 특히 2번(자동 확정 없음·이력 삭제 없음), 12번(maker ≠ verifier,
  선택 스테이징, 커밋 전 보고), **22번(이 체계의 포인터)**.
- **규칙을 고치는 일이면 `규칙한바퀴` 스킬을 먼저 부른다.** 자리 셋은 `.claude/agents/`(운영·구현·독립검토).
- river 보고는 전역 CLAUDE.md §1 — 굵은 결론 한 줄, 쉬운 말, 정할 것은 끝에 목록, **중간 보고 0회**.
  체계 용어(볼 것·양면 시험·문지기·지문)를 그대로 쓰지 말고 매번 풀어 쓴다.
  2026-09-16 실제 피드백 둘: **"무슨 말인지 모르겠다"**, **"쉽게 좀 말해라"**.
  `넣을까요?` 처럼 줄인 말도 되물어 왔다 — 무엇을 어떻게 한다는 것인지 한 줄로 풀어 쓴다.

## 1. 지금 상태 (한눈에)

| 무엇 | 지금 |
|---|---|
| 가지 | `feat/worker-structure` — **main 을 2026-09-17에 끌어와 합쳤다**(검수기 내용은 main 과 같다) |
| 검수기 | `engine/ui.html` 지문 `256035a5` = main 의 것과 같다 |
| 시험 화면 | **22장** (2026-09-17 river 결정으로 앱첫화면·앱비번숨김 두 장 더함) |
| 출발점 성적 | `026-20260917-1633` — **볼 것 440** · 정답 7/7 · 자체검사 249 통과 0 실패 |
| 규칙 장부 | P1~P24. **P21~P24 는 포털 검수기에 이미 들어갔다** · P14·P18·P19·P20 보류 |

**규칙은 이미 실제 검수에 반영돼 있다.** P21·P22 는 2026-09-17 오전에 main 으로 합쳤고
(`caa04ee`), P23·P24 는 main 에서 바로 한 바퀴를 돌려 넣었다. 이 폴더가 뒤처져 있던 것을
오늘 오후에 맞췄다 — 앞으로 여기서 규칙을 고치려면 **먼저 main 을 끌어와 맞춘 뒤** 시작한다.

**옛 숫자(414·421·437·438·441)와 지금 440 을 나란히 놓지 마라** — 시험 화면과 검수기가 달라졌다.
견줄 수 있는 성적의 조건은 **검수기 지문 · 시험지 지문이 같을 때만**이다. 출발점은 `규칙장부/기준성적.md`.

### 무엇이 어디 있나
- `시험지/` — 채점표. 세트(20화면) · 정답(체류시간 7건) · 도구(run.js·answers_check.js) · `기준.md` · `양면/`.
  **구현하는 쪽은 고치지 않는다.** `scripts/시험지보호.py` 훅이 막는다(읽기·복사는 통과).
  늘리고 줄이는 것은 `scripts/시험지늘리기.py`(`--넣는다` / `--뺀다`), 포털에서 화면을 꺼내는 것은
  `scripts/시험화면꺼내기.py`. **river가 정했을 때만 돌린다.**
- `규칙장부/` — `README.md` 에 전부 적혀 있다. 출발점 숫자는 `기준성적.md`. 도구 일곱:
  `자료점검.py` · `공식측정.py` · `양면시험.py` · `규칙살았나.py` · `장부적기.py` · `장부보기.py` · `문지기.py`
- 장부 셋: `규칙장부.jsonl`(사건 한 줄, 상태는 계산) · `측정장부.jsonl`+`측정/<번호>/` · `자료목록.md`

## 2. 규칙 한 건이 가는 길 (2026-09-16·17에 여섯 건을 실제로 돌려 봄)

등록 → **[river] 범위·기준 승인(승인번호)** → 양면 시험지 한 장 → 구현 → 공식 측정 →
양면 시험 → 독립 검토 → 문지기 → **[river] 적용 승인** → 커밋

- 구현·독립검토는 `.claude/agents/` 의 자리로 **따로 띄운다**. 한 손이 만들고 재면 이 체계가 무의미하다.
- 되돌리면 구현은 **두 번까지** 고쳐 다시 잰다. 세 번째는 멈추고 river에게 올린다(P19에서 실제로 멈췄다).
- **양면 시험지는 구현 전에 쓴다.** 나중에 쓰면 만든 것에 맞춰 시험을 쓰게 된다.

## 3. 배운 것 (같은 데서 또 헤매지 않게)

1. **근거 없는 제안은 만들지 마라.** P18·P20은 코드에 구멍이 실재했지만 시험 화면에서
   놓치는 것이 0건이었다. 화면을 21장으로 늘려 다시 재도 0건이었다. 둘 다 접었다.
   → **등록 전에 먼저 재라.** 검수기를 고치지 않고 임시 사본에 세는 줄만 넣어 돌리면 된다(탐침).
2. **구현이 "잡힌다"고 한 것이 공식 측정에서 0건일 수 있다**(P19에서 세 판 내리). 구현에게
   **시험지 도구로 직접 20화면을 돌려 확인하고, 고치기 전 합계가 장부의 출발점과 맞는지부터
   맞춰 보라**고 시켜라. P21·P22·P24는 그렇게 시켜서 한 번에 통과했다.
3. **줄어든 것이 좋은 소식이 아닐 수 있다.** P19의 008은 볼 것을 줄였지만 헛지적 한 장이
   진짜 지적 4장을 덮은 것이었다. 줄면 무엇이 사라졌는지 원본 결과를 맞대 봐라.
4. **눈으로 훑는 것이 가장 잘 먹혔다.** P21·P22는 새 화면을 그림으로 나란히 놓고 보다가 찾은 것이다.
5. **근거를 잘못 짚어 놓고 등록한 일이 실제로 있었다**(P23의 근거 ②). 엔진을 돌려 보지 않고
   시안 나무만 보고 넘겨짚은 것이었다. 독립 검토가 잡아 `바로잡음` 사건으로 쌓았다.
   → **등록 전에 그 화면을 실제로 한 번 돌려 본다.**
6. **양면 시험 도구를 그대로 믿지 마라 — 도구가 못 보는 쪽이 있다.** 2026-09-17에 고친 것이 이것이다(아래).

## 4. 2026-09-17 오후에 한 일 — 규칙이 죽어도 아무도 모르던 자리를 막았다

**문제:** 양면 시험은 시안 글자로 자리를 찾았다. 그래서 '시안에 아예 없는 것'을 찾는 규칙(P22 —
개발에만 생긴 작은 단추)은 **'드러나야 할 것' 쪽을 적을 수가 없었다.** 그 쪽이 비어 있으니
규칙이 죽어도 시험은 '두 쪽 다 맞음'을 내주었다 — P22를 넣기 **전**에 잰 015 에서도 통과였다.

**고친 것 (river 승인 2026-09-17):**
- `규칙장부/도구/양면시험.py` — 한 건에 `"글자"` 대신 `"네모": {x,y,w,h}` 를 적을 수 있다.
  네모는 **개발 촬영본 그림 위의 자리**다. 후보의 개발 좌표(rawBox)가 그 네모 안에 절반 넘게
  들어오면 '그 자리에 있다'고 센다. `"이름표"` 를 함께 적으면 그 말이 든 후보만 세어,
  다른 규칙이 우연히 같은 자리를 짚어 통과하는 것을 막는다. 둘을 함께 적으면 판정 불가.
  못 찾았을 때는 그 자리에 닿은 후보를 겹친 비율과 함께 적어 둔다.
- `시험지/양면/P22.json` — 웹로그인실패 아이디 칸의 지우기 × 단추(개발 1149,234 18×16)를
  네모(1131,216 54×52) + 이름표 '디자인에 없는' 으로 적었다.
- `규칙장부/도구/규칙살았나.py` **새로 만듦** — 있는 양면 시험지 **전부**를 한 성적에 대고
  한 번에 돌려 어느 규칙이 아직 살았는지 한 줄씩 보인다. 재지 않고 남은 결과만 읽는다.
  `python3 규칙장부/도구/규칙살았나.py 020-20260917-1306 [--견줄것 <옛 측정번호>]`

**확인한 것:** 015(P22 없던 판)에 대면 '틀린 것 있음'(그 자리 후보 0건), 016·020(P22 든 판)에
대면 '맞음'(후보 #3 이 100% 들어옴). 옛 양면 시험 19건을 새 도구로 다시 돌려 판정·줄별 결과가
한 글자도 안 달라진 것을 맞대어 확인. 020(지금 검수기)에 대니 **일곱 장 모두 살아 있음.**
고친 두 도구는 `규칙장부/도구/` 에 있고 시험지 지문은 `양면/` 을 빼고 셈하므로
**앞서 잰 성적과 그대로 견줄 수 있다**(`시험지/기준.md` 은 손대지 않았다).

**검수기를 고치면 `규칙살았나.py` 를 함께 돌린다.** 공식측정은 양면 시험을 스스로 돌리지 않는다 —
그래서 이 한 줄을 잊으면 예전 문제로 돌아간다.

### 4-1. 이어서 한 것 — P23 도 죽으면 걸리게 (river 결정 '더줘', 같은 날 저녁)

P23(맨 위·아래 틀 띠를 자식 이름만큼만 좁히는 규칙)은 **짚는 것을 늘리지 않고 헛지적을 없애는**
규칙이라 드러날 새 후보가 원래 없다. 게다가 그 규칙이 닿는 화면이 시험지에 하나도 없었다.

- **시험 화면을 21장으로 늘렸다** — `앱첫화면`(= 우비스 버스 스플래시, 프레임 `218:14376`,
  `UVIS_USER_APP-AND-001` 쪽. 같은 프레임이 삼성통근버스 쪽에도 있어 둘을 다 돌려 보고
  포털에서 마지막으로 검수된 쪽을 골랐다). 넣은 까닭은 숫자가 아니라 **P23 의 채점**이다.
- `시험지/양면/P23.json` 접히는 쪽 첫 줄에 그 자리를 네모(410,1945 290×140)로 박았다 —
  하단 묶음 안 '에스원' 로고 자리. 규칙이 죽으면 그 자리에 헛지적이 되살아나 이 줄이 틀린다.
- **확인:** 규칙을 뺀 판(`022-20260917-1544`, 성적 아님)으로 재면 '틀린 것 있음',
  지금 검수기(`021`)로는 '두 쪽 다 맞음'.
- 새 출발점 `021` — 21화면 볼 것 **438**, 옛 20장은 화면별 숫자까지 `020` 과 전부 같다.

**여기서 드러난 더 큰 사실:** 시험지가 '두 쪽 다 맞음'이라고 해서 그 규칙이 살았다는 뜻이 아니다.
**규칙을 아예 빼고 재도 맞는 시험지가 있다** — P24 가 그렇다(규칙을 뺀 022 로 재도 통과).
그래서 시험지에 칸 하나를 더 두었다: `"규칙 없는 판 확인"` — 그 규칙을 뺀 판으로 재서 이
시험지가 실제로 걸린 성적 번호를 적는다. 도구도 낱말을 바로잡았다('살아 있음' → '맞음').
### 4-2. 일곱 장을 다 확인했다 (river 지시 '확인해줘', 같은 날 밤)

옛 검수기로 되돌리는 대신 **지금 검수기에서 그 규칙 한 줄만 끈 판**을 만들어 쟀다 —
옛 판으로 되돌리면 그 뒤의 다른 조각까지 빠져 무엇 때문에 걸렸는지 못 가린다.

| 시험지 | 결과 | 끈 판 · 걸린 자리 |
|---|---|---|
| P1 | ○ 걸린다 | `023` · route '공지사항'이 가변으로 접힘 (볼 것 437) |
| P17 | ○ 걸린다 | `024` · table 컬럼 제목 '연락처' 길어짐이 사라짐 (볼 것 430) |
| P21 | ○ 걸린다 | `025` · 가입약관 '회원 이용약관 동의(필수)'가 접힘 (볼 것 436) |
| P22 | ○ 걸린다 | `015` · 웹로그인실패 지우기 × 자리 후보 0건 |
| P23 | ○ 걸린다 | `022` · 앱첫화면 로고 자리 헛지적이 되살아남 |
| P24 | ○ 걸린다 | `027` · 앱비번숨김 도구 줄 자리에 후보 여덟이 되살아남 (볼 것 448) |
| P19 | — 가릴 규칙 없음 | 2026-09-16 river 결정으로 접었고 코드를 남기지 않았다 |

**일곱 장 중 여섯 장이 규칙을 붙들고 있다.** 남은 P19 는 규칙 자체가 없어 가릴 것이 없다.
P24 는 river 결정으로 `앱비번숨김`(우비스 버스 '로그인 화면_비밀번호 숨김 상태')을 시험 화면으로
더해 같은 날 채웠다 — 그 화면 볼 것 2건, 규칙을 끄면 10건으로 뛴다(늘어난 8건이 도구 줄 헛지적).
반대 시험 셋(023·024·025)은 자체검사를 건너뛰고 쟀다 — 규칙을 끄면 그 규칙을 지키는
자체검사 줄이 당연히 실패하므로 실패 수를 세는 것이 뜻이 없다. 셋 다 **성적이 아니다.**
자세한 표는 `규칙장부/기준성적.md`. 검수기는 한 글자도 손대지 않았다(끈 판은 임시 자리에만 만들었다).

### 4-3. 포털에서 볼 수 있게 했다 (river 요청 '어디선가 볼 수 있어야', 같은 날 밤)

규칙과 채점이 장부 파일에만 있어 도구를 돌리지 않으면 아무도 볼 수 없었다.
포털 `검수 규칙`(`/policy`) 아래에 **`/policy/쓰는규칙`** 한 장을 더했다(`mvp0/rule_board.py`).

- 맨 위: **지금 검수기로 잰** 성적 한 줄(시험 화면 수·볼 것·정답·자체검사). 검수기가 바뀐 뒤
  다시 재지 않았으면 숫자를 지어내지 않고 "지금 검수기로 잰 성적이 없습니다"라고 말한다.
  (`측정장부.jsonl` 에서 지금 `engine/ui.html` 지문과 같고 자체검사를 건너뛰지 않은 마지막 줄)
- 가운데: **지금 쓰는 규칙 14개** — 쉬운 말 한 줄 + `채점이 지키나`(양면 시험지의
  `규칙 없는 판 확인` 칸을 읽는다). 지금 6개 지켜짐 · 8개는 **채점표가 아예 없다**.
- 아래: 검수기가 화면마다 읽는 규칙표(제외·가변·그림·촬영본 17줄)를 검수기 파일에서 그대로
  읽어 접어 둔다. 표를 못 찾으면 못 찾았다고 말한다.
- **읽기만 한다.** 여기서 규칙을 바꾸지 않는다. DB도 건드리지 않는다.
- 쉬운 말 한 줄은 `rule_board.쉬운말` 에 사람이 쓴 것이다 — 장부에 새 규칙이 올라오고 여기
  줄이 없으면 화면이 '쉬운 말 설명이 아직 없습니다'로 짚는다. **새 규칙을 넣을 때 한 줄 쓴다.**

**이 페이지는 main 에 합쳐야 river 가 실제로 본다** — 포털은 `~/dev-screen-qa`(main)에서 돈다.

## 5. 바로 다음에 할 것 (river가 고르는 순서)

1. **새 규칙을 넣을 때마다 그 시험지의 '규칙 없는 판 확인' 칸을 함께 채운다.** 규칙 한 줄만
   끈 판으로 한 번 재면 된다(자체검사는 건너뛴다). 안 채우면 그 시험지는 합격이 무슨 뜻인지
   모르는 채로 남는다. 지금 일곱 장 중 여섯 장이 채워져 있다.
3. **독립 검토가 올린 반례를 양면 시험지에 넣기.**
   - P21 쪽: 한 칸 값이 줄마다 똑같이 반복되는 긴 자료('서울 강남지점' ×3) / 말꼬리가 같은 자료
     ('생산관리팀·품질관리팀·자재관리팀') / 말투를 안 나눠 쓰는 항목 이름('알림 설정·비밀번호 변경·로그아웃')
   - P22 쪽: 체크된 상자 / 라디오 점 / 토글 켜짐 / 포커스 링 / 입력칸 배경색만 다른 화면
   - P24 쪽: 자판 바로 위 '거의 흰' 앱 단추 띠 / 자판 바탕이 연한 기기 / 도구 줄 위에 또 연한 띠
   셋 다 **시험 화면을 늘려야** 적을 수 있다(river 결정).
4. **P22 가 아직 못 보는 것** — 8px 못 되는 점 · 44px 넘는 큰 배지 · 글줄 모양으로 생긴 것.
5. **앱비번보임 화면의 정렬.** 시안 360x812 와 개발 촬영본 1080x2340 이 안 겹친다(ty=241).
   그래서 시험지에서 뺐다. 그림·요소는 `시험지/세트` 에 그대로 있으니 정렬을 고치면 되돌린다.
   (river 결정으로 뺐던 화면은 함부로 다시 넣지 않는다 — `fc2a9b0`)
6. **P1(표 항목 이름 고정)을 다시 볼 자리.** 표항목 화면을 넣어 보니 볼 것이 table 과 똑같이
   4건인데 접는 규칙만 `role:가변` → `repeat:가변` 으로 바뀐다. 표 본문 값은 이름이 '항목'이
   아니어도 반복 행 규칙이 이미 접는다 — P1이 정말 필요한지 따져볼 근거다.
7. 대기열 0-2(촬영 앞 시안 점검) 등 CLAUDE.md 21번으로 복귀.

## 6. river가 아직 안 정한 것
- 이 가지를 **언제 정리할지**. 규칙은 이미 main 에 있으니 남은 것은 `시험지/`·`규칙장부/`·이 쪽지다.
- 두 번째 승인을 '커밋'에서 **'포털 적용(main 합치기)'** 으로 옮기고 가지 커밋은 보고 없이 둘지.
  안 정하면 지금 규칙(커밋마다 보고).
- 포털의 '제외'를 **'이번만 / 같은 요소에 계속'** 으로 나누기 — 포털 동작 변경이라 이 조각 아님.

## 7. 남은 사실 (숨기지 않고)
- 시험 화면 20장 중 **정답이 달린 것은 체류시간 한 장(7건)뿐**이다. 나머지 19장은 볼 것 수만 센다.
- 새로 넣은 다섯 화면은 볼 것이 2~5건으로 적다. 근거로 쓰기엔 아직 얇다.
- 새 웹 화면 셋(가입약관·웹로그인실패·웹로그인커서)이 **같은 로고 크기 차이**를 함께 짚는다
  (시안 169px · 개발 183px, 8% 큼). 그림으로 확인한 진짜다 — 서로 다른 화면에서 같은 원인이
  나온 것이라, 서비스 공통 규칙을 올릴 때 쓸 근거다.
- `시험화면후보/` 는 깃에 안 올린다(`.gitignore`). 넣고 나면 `시험지/세트` 가 정본이다.
- 공식 측정 중 크롬이 한 번 죽어 화면 하나를 못 잰 적이 있다(014). 그때는 **판정 불가**이고
  다시 재는 것이 맞다 — 빠진 채로 합계를 내지 않는다.
- P23·P24 의 실제 효과는 **시험 화면 20장 밖에서만** 확인됐다(우비스 버스 스플래시·비밀번호
  숨김 상태). 20장 볼 것은 437 로 한 건도 안 움직였다. 그 화면들이 시험지에 없기 때문이다.
- `시험지/기준.md` 의 글에는 아직 '열다섯 화면' · '자체검사 222개'라고 적혀 있다. 고치면 시험지
  지문이 또 바뀌어 `021` 과 못 견주게 되므로 **다음에 시험 화면을 늘릴 때 함께 고친다.**
- river 가 뺀 `앱비번보임` 은 다시 넣지 않았다. `시험지늘리기.py` 가 집어 들지 않게
  `시험화면후보/river가뺀것/` 으로 치워 두었다(그 폴더는 깃에 안 올라간다).
- `문지기.py P22` 는 지금도 문이 닫힌다 — 장부에 'P22 를 지금 검수기로 잰 공식 성적'이 없기
  때문이다(020 은 P24 이름으로 잰 것). P22 는 이미 적용된 규칙이라 막힌 것이 아니다.
