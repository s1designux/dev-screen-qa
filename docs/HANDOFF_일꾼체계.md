# [새 세션용] 검수 일꾼 체계 — 인수인계 (고침 2026-09-16 저녁)

## 0. 먼저 할 일
- 이 폴더 `~/dev-screen-qa-일꾼체계` 에서만 일한다. 가지 `feat/worker-structure`.
  본 폴더 `~/dev-screen-qa` 는 **항상 main** — 다른 세션이 거기서 커밋한다. 거기서 가지를 갈아타지 않는다.
- `CLAUDE.md` 를 전제로 삼는다. 특히 2번(자동 확정 없음·이력 삭제 없음), 12번(maker ≠ verifier, 선택 스테이징, 커밋 전 보고), **22번(이 체계의 포인터)**.
- **규칙을 고치는 일이면 `규칙한바퀴` 스킬을 먼저 부른다.** 자리 셋은 `.claude/agents/`(운영·구현·독립검토).
- river 보고는 전역 CLAUDE.md §1 — 굵은 결론 한 줄, 쉬운 말, 정할 것은 끝에 목록, 중간 보고 없음.
  **2026-09-16 실제 피드백: "무슨 말인지 모르겠다."** 체계 용어(볼 것·양면 시험·문지기·지문)를 그대로 쓰지 말고
  매번 풀어 쓴다 — "검수기가 못 보던 것", "고친 걸 그대로 쓸까요" 수준으로.

## 1. 지금까지 한 것 (①~⑤ 끝, 커밋 5개)

| 커밋 | 무엇 |
|---|---|
| `f7197ba` | 정본 자료함 확정 + 되살리기 시험 |
| `50bcd3c` | 시험지 분리 + 출발점 성적 414 |
| `87147f8` | 자리 셋 + 규칙 장부(손글씨 16건 옮김) |
| `6df7ed0` | 시범 한 바퀴(P1) + 막히는지 확인 |
| `aba258d` | P17 — 글자가 칸 밖으로 길어진 것을 본다 |

**아직 main 에 합치지 않았다.** 합칠지는 river가 정한다.

### 무엇이 어디 있나
- `시험지/` — 채점표. 세트(15화면) · 정답(체류시간 7건) · 도구(run.js·answers_check.js) · `기준.md` · `양면/`.
  **구현하는 쪽은 고치지 않는다.** `scripts/시험지보호.py` 훅이 막는다(읽기·`cp` 원본 쪽은 통과).
  훅은 우회할 수 있으므로 진짜 확인은 문지기의 지문 대조다.
- `규칙장부/` — `README.md` 에 전부 적혀 있다. 도구 여섯:
  `자료점검.py`(정본·되살리기) · `공식측정.py` · `양면시험.py` · `장부적기.py` · `장부보기.py` · `문지기.py`
- 장부 셋: `규칙장부.jsonl`(사건 한 줄, 상태는 계산) · `측정장부.jsonl`+`측정/<번호>/` · `자료목록.md`

### 꼭 기억할 숫자·규칙
- 출발점 성적 `004-20260916-1258`: 화면 15개 **볼 것 414** · 정답 7/7 · 자체검사 222.
  P17 적용 뒤 `007-20260916-1357`: **421** · 7/7 · 227.
- 옛 기록(647·529·440·331·457)은 재는 조건이 제각각이라 **참고 기록**. 지금 숫자와 나란히 놓지 않는다.
- 견줄 수 있는 성적의 조건: **검수기 지문 · 시험지 지문이 같을 때만**.
- 시험지 지문에서 `양면/` 은 뺀다(시험 한 장 늘었다고 옛 성적이 죽으면 안 된다).
- 문지기는 여러 성적 중 **지금 검수기로 잰 것**만 고른다(규칙을 끄고 잰 반대 시험을 성적으로 못 쓴다).

## 2. 규칙 한 건이 가는 길 (실제로 돌려 봄)

등록 → **[river] 범위·기준 승인(승인번호)** → 구현 → 공식 측정 → 양면 시험 → 독립 검토 → 문지기 → **[river] 적용 승인**

- 구현·독립검토는 `.claude/agents/` 의 자리로 **따로 띄운다**(한 손이 만들고 재면 이 체계가 무의미하다).
- 독립 검토가 되돌리면 구현은 **두 번까지** 고쳐 다시 잰다. 세 번째는 멈추고 river에게 올린다.
- 실제로 P17 이 한 번 되돌려졌다(표 본문 값까지 짚어서) → 데이터 자리 막이를 더해 통과.

## 3. 바로 다음에 할 것 (river가 고르는 순서)

1. **글자가 두 줄로 접히는 넘침** — P17 이 `ds.lines>1||cs.lines>1` 이면 건너뛴다.
   개발에서 글자가 길어져 줄이 늘어나는, 가장 흔한 넘침을 아직 못 본다. (독립 검토가 올린 반례)
2. **색이 먼저 걸리면 길이가 묻힌다** — 글자색 차이에서 `return` 해 잉크 폭 검사가 안 돈다. (같은 출처)
3. **반복되는 행 안 붙박이 문구**(행마다 있는 '선택'·'삭제' 같은 버튼 글자)가 바뀌면 드러나야 하는데 지금은 조용하다.
4. **시험지에 화면 더 넣기** — 표가 있는 화면이 한 장뿐이라 표 규칙의 근거가 얕다.
   후보: 노선관리_add(띄어쓰기 3건 · P1 의 원래 근거) · 표 본문 셀 이름이 `항목`인 화면(P1 반례).
   시험지를 늘리면 **출발점 성적을 다시 재야 한다**(옛 성적과 못 견준다).
5. 대기열 0-2(촬영 앞 시안 점검) 등 CLAUDE.md 21번으로 복귀.

## 4. river가 아직 안 정한 것
- 이 가지를 **main 에 합칠지**, 언제 합칠지.
- 두 번째 승인을 '커밋'에서 **'포털 적용(main 합치기)'** 으로 옮기고 가지 커밋은 보고 없이 둘지.
  안 정하면 지금 규칙(커밋마다 보고).
- 포털의 '제외'를 **'이번만 / 같은 요소에 계속'** 으로 나누기 — 포털 동작 변경이라 이 조각 아님.

## 5. 남은 사실 (숨기지 않고)
- 포털 자료함의 자동 후보 1,239건 중 **P1(`항목`) 규칙이 걸린 자리는 0건**이다 — 표가 있는 화면을 아직 포털에 안 올렸다.
- 그림방(`uploads`) 469장 중 167장은 자료함이 가리키지 않는다. 지우지 않았다.
- 본 폴더에는 **아직 커밋되지 않은 검수기 수정**(세로 정렬을 윤곽으로 다시 보는 조각)이 있다. 오늘 성적은 그것을 넣지 않은 판이다.
- `규칙장부.jsonl` 의 `틀시험` 줄 네 개는 문지기·양면 틀이 도는지 본 점검 기록이다(진짜 제안 아님, 바로잡음 사건으로 표시해 둠).
