# 정본 시험용 — 진짜 정본이 아니다

자가검사(`python3 -m valueqa.selftest` 9·10번)가 **오프라인에서 매번 같은 답**을 내려고 만든 아주 작은 흉내다.
진짜 정본은 `github.com/s1designux/S1-UX-DESIGN-with-AI` 이고, 검수할 때는 **그때그때 받아온다**(`--정본 auto`).
여기 값을 보고 판정하지 않는다. 정본의 파일 **모양**(어디에 무엇이 있는지)만 같게 두어 읽는 길을 시험한다.
