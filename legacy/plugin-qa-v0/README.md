# 옛 검수 플러그인 (첫 판) — 사용 안 함

2026-09-14: 검수는 포털에서만 한다(CLAUDE.md 4번). 이 판(`code.js` / `ui.html`)은 이력으로만 둔다.
최상위 `manifest.json` 은 2026-09-14 치웠다 — 플러그인은 `capture-app/plugin-pick/` 안에 제 폴더로 하나만 둔다.
(이름이 같은 것이 두 곳에 있어 피그마 등록 때 헷갈렸다.)
